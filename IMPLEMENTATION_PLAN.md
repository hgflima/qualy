# Implementation Plan

> Gerado por Ralph (planning mode) com base em `.harn/docs/mvp/SPEC.md` e `.harn/docs/mvp/PLAN.md`.
> Estado atual confirmado por `find src/` (vazio): nada implementado. Todas as fases do PLAN.md ainda são gaps.
> Convenção arquitetural (PLAN.md): CLI determinístico em `cli/src/` invocado por harness fino (`skills/`, `commands/`, `agents/`). Runtime: Node ≥ 22.6 com `--experimental-strip-types` (sem build step).

---

## Priority 1: Fase 0 — Bootstrap do workspace

- [x] Criar `package.json` raiz com campos `name="qualy"`, `private:true`, `type:"module"`, `engines.node:">=22.6.0"`, scripts placeholder (`test`, `test:e2e`) (why: PLAN §Fase 0; pré-condição para qualquer outro comando npm/pnpm)
- [x] Criar `tsconfig.json` raiz com `target:"ES2023"`, `module:"NodeNext"`, `moduleResolution:"NodeNext"`, `strict:true`, `noEmit:true` (why: PLAN §Fase 0; viabiliza `tsc --noEmit` da validação por iteração em AGENTS.md)
- [x] Criar `cli/package.json` com deps runtime mínimas (`ts-morph`, `vitest`, `chart.js`, `chartjs-plugin-treemap`, `esbuild`) (why: PLAN §Critical files; declara dependências em isolado do harness)
- [x] Criar `cli/tsconfig.json` estendendo o raiz, `rootDir:"./src"` (why: PLAN §File layout; isolamento de paths)
- [x] Criar `cli/src/lib/exit-codes.ts` exportando constantes (`OK=0`, `RECOVERABLE_ERROR=1`, `UNSUPPORTED_STACK=2`, `DIRTY_TREE=3`, etc.) (why: PLAN §Contratos CLI – exit codes semânticos documentados)
- [x] Criar `cli/src/lib/logger.ts` que escreve mensagens estruturadas em stderr e JSON puro em stdout (why: PLAN §Princípios – CLI emite JSON em stdout, erros em stderr)
- [x] Criar `cli/src/lib/json.ts` com `parseDefensive` e `stringifyPretty` para evitar throws não-tratados (why: PLAN §File layout – utilitário compartilhado)
- [x] Criar `cli/src/index.ts` como dispatcher por `process.argv[2]` listando subcomandos vazios e implementando `--help`, `--version` (why: PLAN §Fase 0 verificação; hub que viabiliza chamar qualquer subcomando posteriormente)
- [x] Criar `vitest.config.ts` raiz apontando para `cli/tests/unit/**` (why: AGENTS.md – tests via vitest; PLAN §Verification)
- [x] Hoistar `vitest` (e `typescript`) para um lugar onde `vitest.config.ts` na raiz consiga resolver o pacote — escolhida opção (b): npm workspaces (`"workspaces": ["cli"]` no `package.json` raiz). `npm install` na raiz hoista `vitest`/`typescript` para `node_modules/` da raiz; `cli/node_modules` e `cli/package-lock.json` removidos (lockfile agora vive só na raiz). Scripts raiz: `test → vitest run`, `typecheck → npm run typecheck --workspace @qualy/cli`. `npm test` executa 25/25 unit tests; `npm run typecheck` passa limpo (why: AGENTS.md constraint; desbloqueia execução real dos unit tests da Fase 1+)
- [x] Corrigir nome do pacote em `cli/package.json` de `chartjs-plugin-treemap` (404) para `chartjs-chart-treemap@^3.1.0` (why: pacote real publicado; desbloqueia `npm install --prefix cli`)
- [x] Adicionar `typescript` em `cli/devDependencies` para que `npm run typecheck --prefix cli` (`tsc -p tsconfig.json --noEmit`) tenha o binário disponível (why: script existia em `cli/package.json` mas faltava a dep)
- [x] Criar `.gitignore` raiz cobrindo `node_modules/`, artefatos de build/coverage, `.lint-audit/`, `.lint-backup/`, `.lint-manifest.json`, `ralph.log`, `.ralph_stuck_tracker` (why: agora que `npm install --prefix cli` funciona, evita commitar `cli/node_modules/`)
- [x] Criar `install.sh` no raiz que valida `node --version` ≥ 22.6 e copia/symlinka `skills/`, `commands/`, `agents/`, `cli/` para `~/.claude/` (idempotente). Modos: `copy` (default) e `--dev` (symlink). Layout: `skills/<name>/` e `commands/<name>/` por sub-diretório, `agents/*.md` por arquivo, `cli/` aninhado em `<target>/skills/lint/cli` (consistente com pattern `${CLAUDE_PLUGIN_ROOT}/skills/lint/cli/src/index.ts` do PLAN §Resolução do CLI). Verificações manuais: Node 22.5 → exit 1 com mensagem clara; Node 22.6/24.x → ok; segunda execução → idempotente; `--dry-run` não toca FS. Guarda defensiva `assert_safe_target` recusa qualquer remoção fora de `$TARGET_ROOT/<...>` (why: PLAN §Fase 0 + ADR 0009 – distribuição manual via script)
- [x] Criar `docs/adrs/0006-deterministic-cli-thin-harness.md` registrando a decisão central — duas camadas (CLI determinístico em `cli/src/` + harness fino em `.md`), princípios (stdout JSON, exit codes semânticos, idempotência via `.lint-manifest.json`, sem perguntas no CLI), exceção autorizada do `lint-auditor` para `rationale` (cross-ref ADR 0008), tradeoffs e verificação por fixtures vitest. Referencia ADR 0007/0008/0009. (why: PLAN §Fase 0 lista ADRs obrigatórios)
- [x] Criar `docs/adrs/0007-runtime-ts-strip-types.md` registrando: Node ≥ 22.6 + `--experimental-strip-types` como runtime único (sem `tsx`/`ts-node`/`dist/`), pattern de invocação fixo no `SKILL.md`, restrições de sintaxe TS impostas pelo strip-types e reforçadas por `verbatimModuleSyntax`/`isolatedModules`/`allowImportingTsExtensions` no `tsconfig.json` raiz, `esbuild` autorizado só dentro de `report-export` (não para o CLI próprio), tradeoffs (floor de Node alto, subset de TS) e verificação via `package.json#engines`, guarda no `install.sh`, `npm run typecheck` e `npm test`. Cross-refs ADR 0006/0009. (why: PLAN §Fase 0 lista ADRs obrigatórios; justifica Node 22.6+)
- [x] Criar `docs/adrs/0009-install-script-distribution.md` registrando: distribuição manual via `install.sh` (Bash) com modos `copy` (default) e `--dev` (symlink), validação de Node ≥ 22.6 antes de qualquer escrita, layout específico (`cli/` em `<target>/skills/lint/cli/`), idempotência, guarda `assert_safe_target`, `--dry-run`, `--target`, exclusão de desinstalação self (delegada ao usuário). Cross-refs ADR 0006/0007. Alternativas: plugin nativo (postergado v2), `npm i -g`, instruções manuais, instalador Node, Make/Just, `install.ps1` (why: PLAN §Fase 0 lista ADRs obrigatórios; explica `install.sh` em vez de plugin nativo em v1)

## Priority 2: Fase 1 — Detecção (read-only)

- [x] Criar `cli/src/lib/git.ts` com wrappers (`isClean`, `firstCommitDate`, `churn90d`, `lsFilesByExt`) chamando `git` via `child_process` (why: PLAN §File layout; base usada por múltiplos detectores). Implementado com `execFileSync` (sem shell), seam testável `setGitRunner`, e retorno `GitResult<T>` discriminado para que detectores tratem repos vazios e falhas sem throw. 19 testes unit cobrindo paths felizes, repo vazio, datas inválidas, dedup de pathspecs e validação de extensões. Convém: `firstCommitDate` retorna `null` para repos sem commits; `churn90d` 0; `lsFilesByExt` recusa extensões com `/`, `\` ou whitespace.
- [x] Criar `cli/src/lib/pkg-manager.ts` que detecta gerenciador a partir do lockfile (`bun.lockb` → bun, `pnpm-lock.yaml` → pnpm, `yarn.lock` → yarn, default npm) (why: AGENTS.md – detecção por lockfile). Implementado com prioridade `bun.lock` > `bun.lockb` > `pnpm-lock.yaml` > `yarn.lock` > `package-lock.json` > default; retorna `{ manager, source }` para que `status` distinga "lock encontrado" de "fallback npm". Test seam `setExistsFn`. 11 testes unit cobrindo cada lockfile, prioridade, curto-circuito e default.
- [x] Implementar `cli/src/commands/detect-stack.ts` (lê `package.json`, classifica `.ts/.tsx/.js/.jsx` como suportado; resto bloqueia) (why: SPEC §1 stack suportada – única lista que oxc cobre). Implementado como detector puro `detectStack({ cwd }, { existsFn? })` + `runDetectStack(argv)` handler. Combina dois sinais: (i) extensões via `lsFilesByExt` (ts/tsx/js/jsx supported, vue/svelte → blocker) e (ii) marcadores de linguagem via `existsSync` no root (`pyproject.toml`/`setup.py`/`Pipfile`, `go.mod`, `Cargo.toml`, `Gemfile`, `composer.json`, `pom.xml`, `build.gradle`(.kts), `mix.exs`). Decisão: `supported = blockers.length===0 && (ts+tsx+js+jsx) > 0`. Exit codes: 0 supported, 2 UNSUPPORTED_STACK (qualquer blocker ou zero arquivos TS/JS), 1 RECOVERABLE_ERROR (ex: não é repo git). Wired no dispatcher via `HANDLER_OVERRIDES` Map (substitui notImplemented por subcomando, padrão para próximas fases). 18 unit tests cobrindo: classification TS-only/JS-only, blockers Python/Go/Rust/Vue/Svelte, multi-blocker, sem arquivos+sem blocker, falha git, contagem `.tsx` distinta de `.ts`, parser de flags `--cwd`/`--help`/erros. Smoke test contra este repo: `supported:true`, `tsFiles:12`, exit 0; contra fixture sintético com pyproject.toml: `supported:false`, blocker `python`, exit 2.
- [x] Implementar `cli/src/commands/git-clean-check.ts` (why: SPEC §6 Always – exigir tree limpa antes de mudanças). Adicionado `dirtyFiles(cwd)` em `cli/src/lib/git.ts` que executa `git status --porcelain=v1 -z` e parseia tokens NUL-terminados (cobre paths com espaços; pula token de origem em rename/copy `R`/`C`). Comando `gitCleanCheck()` puro retorna `{ clean, dirtyFiles[] }`; handler emite `{ clean, dirty_files }` em stdout. Exit codes: `OK` (0) limpo, `DIRTY_TREE` (3) sujo, `RECOVERABLE_ERROR` (1) falha de git. Wired no dispatcher via `HANDLER_OVERRIDES`. 14 unit tests novos (4 para `dirtyFiles`, 10 para command): clean tree, paths com espaço, rename/copy skip, falha git, parse de flags `--cwd`/`--help`/erros, contrato JSON do stdout, todos os exit codes. Smoke: contra este repo (sujo) → exit 3 + `dirty_files` listando os arquivos modificados.
- [x] Implementar `cli/src/commands/detect-existing-linter.ts` (procura `.eslintrc*`, `.prettierrc*`, `biome.json`, `dprint.json`, devDeps em package.json) (why: SPEC §1.2 substituição com consentimento). Implementado como detector puro `detectExistingLinter({ cwd }, { existsFn?, readFileFn? })` + handler `runDetectExistingLinter(argv)`. Catálogo `TOOLS` enumera 4 ferramentas: `eslint` (linter), `prettier` (formatter), `biome` (both), `dprint` (formatter); cada tool combina três sinais — config files no root (probe explícito por nome, sem glob), `package.json` keys inline (`eslintConfig`, `prettier`) emitidas como path virtual `package.json#<key>`, e `pkgNames` checados contra `dependencies`/`devDependencies`/`peerDependencies`/`optionalDependencies`. Biome aparece em ambas as arrays. Tools com zero evidência são omitidos. `package.json` malformado / não-objeto / ausente é tratado como "sem evidência" sem throw (parseDefensive + try/catch em `defaultReadFile`). Stdout JSON: `{ linters, formatters }` (contrato PLAN §Contratos CLI). Exit code: sempre `OK` em sucesso (read-only); `USAGE_ERROR` em flag desconhecida. Wired no dispatcher via `HANDLER_OVERRIDES`. 24 unit tests cobrindo: empty repo, ESLint legacy/flat config/inline/devDep, Prettier configs+inline, Biome em ambas as arrays, dprint, multi-tool brownfield, dedup de múltiplos eslint configs, malformed package.json, peer/optional deps, parser de flags. Smoke test: contra fixture sintético com ESLint+Prettier+Biome+inline-prettier → output canônico esperado; contra este repo → arrays vazias.
- [x] Implementar `cli/src/commands/detect-test-runner.ts` (procura `vitest.config.*`, `jest.config.*`, devDeps; lê thresholds atuais se houver) (why: SPEC §3 estratégia coverage – detectar antes de propor). Detector puro `detectTestRunner({ cwd }, { existsFn?, readFileFn? })` + handler `runDetectTestRunner(argv)`. Catálogo `RUNNERS` cobre vitest (configs `vitest.config.{ts,mts,cts,js,mjs,cjs}` + workspace variants + `package.json#vitest` + dep `vitest`) e jest (configs `jest.config.{ts,mts,cts,js,mjs,cjs,json}` + `package.json#jest` + dep `jest`). `vite.config.*` deliberadamente NÃO probed (false positives em projetos pure-Vite sem testes). Threshold reading com duas estratégias: (i) JSON exato (parseDefensive) para `*.json` configs e `package.json#jest` — extrai `coverageThreshold.global.{lines,functions,branches,statements}` para jest e `test.coverage.thresholds.*` ou `coverage.thresholds.*` para vitest; (ii) regex best-effort para JS/TS configs, escopado a containers conhecidos (`thresholds`/`coverageThreshold`/`global`) com janela de ~600 chars para evitar grab de `lines: 80` orfãos. Tie-break runner: score = `configs.length*2 + (pkg_dep?1:0)`; vitest vence em empate (SPEC §3 default recomendado). Output: `{ runner, candidates: { vitest, jest }, coverage: { configured, current_thresholds, current_values:null, source } }`. Exit codes: sempre `OK` em sucesso (read-only); `USAGE_ERROR` em flag desconhecida. Wired no dispatcher via `HANDLER_OVERRIDES`. 25 unit tests cobrindo: empty repo, vitest/jest via config-only/dep-only, jest thresholds via JSON + JS regex + `package.json#jest` inline, vitest thresholds via TS regex + workspace.json, threshold=null quando container ausente, isolamento de `lines:` órfão fora de container, tie-break vitest vs jest (config strength + default preference), malformed package.json, peerDependencies, valores não-numéricos viram null, parser de flags. Smoke test: contra este repo → `runner:"vitest"`, `configs:["vitest.config.ts"]`, `current_thresholds:null` (vitest config não tem coverage.thresholds), `configured:true`.
- [x] Implementar `cli/src/commands/detect-stage.ts` aplicando heurística greenfield/brownfield/legacy do SPEC §3 + emitir sinais brutos (why: SPEC §3 heurística estágio). Detector puro `detectStage({ cwd }, { existsFn?, readFileFn?, now? })` + handler `runDetectStage(argv)`. Coleta seis sinais brutos: (i) idade do repo via `firstCommitDate` (null em repo vazio = age_days null, tratado como "<6mo" pela classificação), (ii) LOC total via contagem de `\n` (semântica `wc -l`, evita off-by-one de `split('\n').length`) sobre `git ls-files '*.ts' '*.tsx' '*.js' '*.jsx'`, (iii) source_files (count), (iv) churn 90d via `churn90d`, (v) has_tests por probe de `test/`/`tests/`/`__tests__/` no root + delegação a `detectTestRunner` (vitest/jest), (vi) TODO/FIXME/HACK density via regex `\b(TODO|FIXME|HACK)\b` (word-boundary evita matches em `todoList`) normalizado por 100 LOC. Plus linter_present via `detectExistingLinter`. Constantes em `STAGE_THRESHOLDS` exportadas: GREENFIELD_MAX_AGE_DAYS=183, GREENFIELD_MAX_LOC=5000, LEGACY_MIN_AGE_DAYS=1095, LEGACY_MIN_LOC=50000, LEGACY_MAX_TODO_DENSITY_PER_100_LOC=1.0. Classificação determinística em prioridade: greenfield (age<183 AND LOC<5k AND no linter) → legacy (age>1095 AND (LOC>50k OR TODO density>1/100 OR no tests)) → brownfield-moderate. Output inclui `reasoning` legível para auditoria (SPEC §6 Always – "justificar com sinais brutos"). Exit codes: 0 success, 1 RECOVERABLE_ERROR (git fail), 4 USAGE_ERROR (flag inválida); read-only nunca atualiza FS. Wired no dispatcher via HANDLER_OVERRIDES. 18 unit tests cobrindo: greenfield default, brownfield (linter prévio em repo jovem, mid-age mid-size), legacy via 3 triggers separados (LOC>50k, no tests, TODO density>1/100), idade só não é suficiente para legacy, repo vazio → greenfield, falha de ls-files, has_tests via vitest config sem dirs, todo_density_per_100_loc=null em LOC=0, word-boundary do TODO regex (`todoList` não matcheia), linter detectado via devDependencies, parser de flags. Smoke test contra este repo: `stage:"greenfield"`, `loc:3583`, `source_files:20`, `linter_present:false`, `has_tests:true`, exit 0.
- [x] Implementar `cli/src/commands/status.ts` agregando versões instaladas, presets ativos, estágio detectado, hooks, coverage threshold, tema do report (why: SPEC §2 `/lint:status`). Aggregator puro `status({ cwd }, { existsFn?, readFileFn?, now? })` + handler `runStatus(argv)`. Compõe `detectStage` + `detectTestRunner` (ambos read-only) e adiciona quatro probes de FS: (i) versões instaladas via `node_modules/<pkg>/package.json#version` (oxlint, oxfmt, `@oxc-project/quality-metrics`, vitest, jest — `null` quando não instalado; deliberadamente NÃO faz fallback ao range declarado em `package.json` para honrar SPEC §2 "versões instaladas"); (ii) presets ativos `oxlint.fast.json` / `oxlint.deep.json` no root; (iii) hooks: `.claude/hooks/post-edit.sh`, settings hook (substring match em `.claude/settings.json` por `post-edit.sh` — robusto a variações de schema), `.husky/pre-commit`, lint-staged (10 nomes de config + `package.json#lint-staged` inline); (iv) tema lido de `.lint-manifest.json#theme` com fallback `"linear-design-md"` (SPEC §4 default). Resiliência: nenhum probe lança; `detectStage` falhando deixa `stage:{detected:null,reasoning:null,signals:null}` mas o resto do payload é emitido com exit `OK`. Output JSON `{ ok, cwd, versions, presets, stage, hooks, coverage, theme }` (PLAN §Contratos CLI). Exit codes: `OK` em sucesso, `USAGE_ERROR` em flag inválida. Wired no dispatcher via `HANDLER_OVERRIDES`. 27 unit tests cobrindo: defaults vazios, leitura de versões instaladas, package.json malformado, presets fast/deep individuais e combinados, hook scripts isolados (post-edit, husky, lint-staged via arquivo e inline), substring match no settings.json, file-based lint-staged > package.json em conflito, malformed package.json não derruba probe, delegação a detectTestRunner para vitest config-only e jest com thresholds inline, delegação a detectStage (greenfield default), fallback `stage=null` em ls-files fail, theme override do manifest, manifest malformado e theme não-string fallback, parser de flags (default cwd, --cwd, --help, --zonk), handler emite JSON único e exit 0, USAGE_ERROR + payload em flag inválida. Smoke test contra este repo: `stage:"greenfield"`, `vitest:"2.1.9"`, `coverage.runner:"vitest"`, `theme:"linear-design-md"`, exit 0.
- [x] Criar fixture `cli/tests/fixtures/greenfield-ts/` com `.git/`, `package.json` minimalista, ~5 arquivos `.ts`, sem linter (why: SPEC §5 T1 + §7.1 acceptance). Implementado como árvore estática: `package.json` minimalista (zero deps, `type:"module"`, `engines.node:">=22.6.0"`) + 5 fontes TS (`src/{index,calculator,user,storage,utils}.ts`) totalizando 420 LOC via semântica `wc -l` (mesma usada em `detect-stage.ts:140`), zero TODO/FIXME/HACK, sem qualquer config de linter/formatter/test runner. `EXPECTED.md` documenta o contrato esperado de cada detector (`detect-stack`, `detect-existing-linter`, `detect-test-runner`, `git-clean-check`, `detect-stage`, `status`) e o protocolo de materialização do `.git/` (init+add+commit em tmp dir) — `.git/` não pode ser tracked dentro do parent qualy repo, então materialização fica para a task do helper `tests/fixtures/_materialize.ts` (futura). Verificação adicional: `tsconfig.json` raiz só inclui `cli/src/**/*.ts`, e `cli/tsconfig.json` exclui `tests/fixtures` — fixtures não entram no typecheck nem no vitest (`include: ["cli/tests/unit/**/*.test.ts"]`); 187/187 unit tests verdes pós-adição.
- [x] Criar fixture `cli/tests/fixtures/brownfield-eslint-prettier/` com ESLint + Prettier configurados, ~5k LOC sintéticos (why: SPEC §5 T1 + §7.2 acceptance). Implementada como árvore estática (mesmo padrão do `greenfield-ts/`): 8 fontes TS em `src/` (`index`, `calculator`, `storage`, `users`, `router`, `parser`, `validator`, `utils`) totalizando 5071 LOC via semântica `wc -l` — deliberadamente acima do teto greenfield (5000) para que classificação não colapse a greenfield mesmo se evidência `linter_present` fosse ignorada. Zero `TODO`/`FIXME`/`HACK` (verificado via `grep -E '\b(TODO|FIXME|HACK)\b'`) → `todo_density_per_100_loc=0`, isolando a classificação ao sinal de linter+LOC. Configs: `.eslintrc.json` (ESLint legacy form, extends `eslint:recommended` + `@typescript-eslint/recommended` + `prettier`) + `.prettierrc.json`. `package.json` declara `eslint`, `@typescript-eslint/eslint-plugin`, `@typescript-eslint/parser`, `eslint-config-prettier`, `prettier`, `typescript` em `devDependencies` (sem `dependencies`, sem test runner). Smoke test contra a fixture: `detect-existing-linter --cwd .../brownfield-eslint-prettier` → `{linters:[{name:"eslint",configs:[".eslintrc.json"],pkg_dep:true}],formatters:[{name:"prettier",configs:[".prettierrc.json"],pkg_dep:true}]}` (match exato com `EXPECTED.md`). `EXPECTED.md` documenta contrato de cada detector (`detect-stack`, `detect-existing-linter`, `detect-test-runner`, `git-clean-check`, `detect-stage`, `status`) após materialização git, e o protocolo de `git init+add+commit` em tmp dir (mesmo do greenfield-ts, futuro `_materialize.ts`). Verificação adicional: `tsconfig.json` raiz + `cli/tsconfig.json` excluem `tests/fixtures` e vitest `include` é só `cli/tests/unit/**/*.test.ts` — fixture não entra em typecheck nem em tests; 187/187 unit tests verdes pós-adição. Backpressure 4/4 (typecheck, lint, tests, build).
- [x] Criar fixture `cli/tests/fixtures/legacy-monorepo/` (pnpm workspace, 3 packages, churn alto via commits sintéticos) (why: SPEC §5 T1 + §7.11 perf <30s). Implementada como árvore estática (mesmo padrão das outras fixtures): `pnpm-workspace.yaml` + root `package.json` (private, `packageManager:"pnpm@9.12.0"`) + 3 packages (`@legacy-monorepo/{auth,core,api}`) com 13 fontes TS totalizando **11468 LOC** (SPEC §7.11 — 10k+ LOC), distribuídas em barrels `index.ts` + 10 módulos de implementação (jwt/session/permissions, store/events/scheduler/cache, router/handlers/middleware). `api` declara workspace deps em `auth`/`core` via `workspace:*`. **164 TODO/FIXME/HACK markers** (densidade 1.43/100 LOC, acima do limiar legacy 1.0). Sem test runner config, sem linter/formatter, sem qualquer dep de produção. Geradas via script one-shot em `/tmp/` (não commitado): código repetitivo mas plausível (interfaces estruturalmente idênticas + classes mutator + helpers puros), suficiente para byte-level probes dos detectores. Smoke contra fixture materializada: `detect-stack`→`supported:true,ts:13`; `detect-existing-linter`→`linters:[],formatters:[]`; `detect-test-runner`→`runner:"none"`; `git-clean-check`→`clean:true`; `detect-stage` com `now=commit_date` → `brownfield-moderate` (LOC>5k mas age=0); com `now=commit+1500d` → `legacy` (age>1095 AND TODO density 1.43>1 AND no_tests, ambos disjuntos legacy disparam). Caveat de churn documentado em `EXPECTED.md`: `_materialize.ts` produz commit único, então `churn_90d=0`; multi-commit history fica para extensão futura do helper (ou injeção de gitRunner em testes que precisem do sinal). **`EXPECTED.md`** documenta os 6 detectores em ambos cenários (age=0 e age>1095) + caveat de churn + nota explícita de que LOC=11468 NÃO dispara o disjunto legacy de LOC>50k (preservado para fixture maior futura). Verificação: fixtures excluídas do typecheck (`cli/tsconfig.json#exclude:["tests/fixtures"]`) e do vitest (`include:["cli/tests/unit/**/*.test.ts"]`); 203/203 unit tests verdes (1 novo em `materialize.test.ts` validando shape: pnpm-workspace.yaml + 3 package.json + index.ts por package + workspace:* deps em api).
- [x] Criar fixture `cli/tests/fixtures/jest-with-coverage/` com `jest.config.js` contendo `coverageThreshold.global.lines=60` (why: SPEC §7.3 acceptance – preserva escolha do usuário). Implementada como árvore estática (mesmo padrão de `greenfield-ts/`, `brownfield-eslint-prettier/`, `unsupported-python/`): 3 fontes TS em `src/` (`index`, `math`, `string-utils`) + 1 teste em `__tests__/math.test.ts` + `jest.config.js` CommonJS no root. Total 130 LOC via `wc -l` semantics; zero TODO/FIXME/HACK. `package.json` declara `jest`, `ts-jest`, `@types/jest`, `typescript` em devDependencies, **sem** `"type": "module"` (jest config é CommonJS — caminho realista de brownfield). `jest.config.js` define `coverageThreshold.global.lines: 60`, exercitando o branch text-regex (`readThresholdsFromText`) de `detect-test-runner` (vs JSON parse de `*.json` ou `package.json#jest`). Smoke contra fixture materializada (`git init` + commit determinístico): `detect-test-runner` retorna `runner:"jest"`, `jest.thresholds:{lines:60,...}`, `coverage.source:"jest.config.js"`, exit 0 — match exato com `EXPECTED.md`. `detect-stack` retorna `ts:4, js:1` (4 ts files + jest.config.js), `supported:true`, exit 0. `EXPECTED.md` documenta contrato dos 6 detectores e nota que `detect-stage` retorna `greenfield` apenas quando `now` é injetado como commit date (com `now` real, age_days excede 183 → cai para `brownfield-moderate` por default classification — convenção idêntica à de `greenfield-ts/EXPECTED.md`). Verificação adicional: fixtures excluídas do typecheck e do vitest (mesmo wiring); 202/202 unit tests verdes pós-adição (1 novo teste em `materialize.test.ts` validando shape + threshold survive copy). Backpressure 2/2 (typecheck, tests).
- [x] Criar fixture `cli/tests/fixtures/unsupported-python/` (`pyproject.toml`, sem TS) (why: SPEC §7.4 acceptance – recusa explícita). Implementada como árvore estática (mesmo padrão de `greenfield-ts/` e `brownfield-eslint-prettier/`): `pyproject.toml` no root (PEP 518/621, hatchling backend) + 3 fontes Python em `src/unsupported_python/` (`__init__.py`, `main.py`, `utils.py`). Sem `package.json`, sem `.git/` (materialização por tmp como nos outros fixtures), sem qualquer config JS/TS. `EXPECTED.md` documenta o contrato dos 6 detectores: `detect-stack` → exit `UNSUPPORTED_STACK` (2) com `blockers:[{kind:"python",file:"pyproject.toml"}]` e `signals.hasPackageJson:false`; `detect-existing-linter`, `detect-test-runner`, `git-clean-check`, `detect-stage` (informational — gate é `detect-stack`; classificaria como greenfield com LOC=0/`todo_density=null`), `status` exit OK. Smoke test contra fixture materializada: contratos batem 1:1 com `EXPECTED.md` para os 6 detectores. Verificação adicional: fixtures excluídas do typecheck e do vitest (mesmo wiring dos outros fixtures); 187/187 unit tests verdes pós-adição. Backpressure 4/4 (typecheck, lint, tests, build).
- [x] Criar `cli/tests/fixtures/_materialize.ts` helper (`materializeFixture(name): { dir, cleanup }`) que copia uma fixture de `cli/tests/fixtures/<name>/` para `mktemp -d`, roda `git init` + `git add -A` + commit determinístico (autor `fixture@qualy.local`, data fixa via `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE`), e devolve cleanup que `rm -rf` o tmp dir. Implementado: API `materializeFixture(name, { commitDate? }): { dir, cleanup }` em `cli/tests/fixtures/_materialize.ts`. Resolve `cli/tests/fixtures/<name>/` via `import.meta.url`, valida shape do nome (rejeita `..`, `/`, `\`, vazio), `mkdtempSync` em `os.tmpdir()` com prefixo `qualy-fixture-<name>-`, `cpSync` recursivo com filtro que **omite `EXPECTED.md`** (documentação, não parte do estado simulado), depois invoca git via `execFileSync` (sem shell) em três passos: `git init -q --initial-branch=main`, `git add -A`, e commit com `--allow-empty` + `-c user.email=fixture@qualy.local` + `-c user.name=fixture` + env `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE`/`GIT_AUTHOR_NAME`/`GIT_AUTHOR_EMAIL`/`GIT_COMMITTER_NAME`/`GIT_COMMITTER_EMAIL` (evita interferência de config global do user). `commitDate` default `2025-01-01T00:00:00Z`; reproduzir hash exato. `cleanup()` re-resolve o path e refuses se não estiver sob `os.tmpdir()` (guarda contra mutação por callers); `rmSync({recursive:true,force:true})` é idempotente. Erros de cp ou git triggeram `safeCleanup` antes de re-throw para não vazar tmp dirs. 14 unit tests cobrindo: cópia para tmp dir único, filtro de EXPECTED.md, preservação byte-a-byte de package.json, repo em branch main com 1 commit pelo fixture author, datas default e custom (validadas via `Date#toISOString` em vez de comparar `%aI` literal — git preserva o `Z` do input), working tree limpo pós-commit, dirs independentes entre chamadas, cleanup idempotente, materialização de brownfield-eslint-prettier (configs `.eslintrc.json`/`.prettierrc.json` sobrevivem), unsupported-python (sem package.json, com pyproject.toml + .py files), rejeição de nomes inválidos (`..`, `foo/bar`, `foo\\bar`, vazio), fixture inexistente. Verificação: 201/201 unit tests verdes; backpressure 4/4 (typecheck, lint, tests, build) (why: descoberto enquanto criando `greenfield-ts/` — `.git/` não pode ser tracked dentro do parent repo, então fixtures precisam ser materializadas em tmp; pré-requisito para a task seguinte de testes contra fixtures)
- [x] Escrever testes unitários vitest para cada detector contra cada fixture relevante (why: PLAN §Fase 1 verificação – tests/unit por detector). Implementado em `cli/tests/unit/detectors-fixtures.test.ts` como suite de integração: cada `it` materializa uma fixture via `materializeFixture()` e exercita um detector contra o tmp dir resultante (cp + `git init` + commit determinístico no helper de Fase 1). 31 testes (= 5 fixtures × 6 detectores, menos um teste duplicado de status para greenfield-ts), distribuídos em 5 `describe` blocks: `greenfield-ts/` (6 — `detect-stack` 5×.ts/no-blockers, `detect-existing-linter` empty, `detect-test-runner` runner=`none`, `git-clean-check` clean, `detect-stage` greenfield com `now=commitDate`, `status` greenfield+default theme), `brownfield-eslint-prettier/` (6 — eslint linter + prettier formatter ambos com `pkg_dep:true`, stage=`brownfield-moderate` por LOC≥5k AND linter_present), `jest-with-coverage/` (6 — `detect-stack` ts=4/js=1, `detect-test-runner` runner=`jest` + thresholds.lines=60 via text-regex em `jest.config.js`, stage=`greenfield` por idade<6mo AND LOC<5k AND no linter, `status` propaga thresholds via `coverage.current_thresholds`), `legacy-monorepo/` (7 — Scenario A com `now=commitDate` → `brownfield-moderate` por LOC>5k AND age<3y, Scenario B com `now=commit+1500d` → `legacy` via age>1095 AND TODO density 1.43>1 AND no_tests; ambos os disjuntos legacy validados), `unsupported-python/` (6 — `detect-stack` exit `UNSUPPORTED_STACK` shape com blockers=[python pyproject.toml] e hasPackageJson=false, demais detectores informacionais conforme `EXPECTED.md`). Determinismo: `materializeFixture` pinna commit em `2025-01-01T00:00:00Z`; `nowAtCommit = () => new Date(COMMIT_DATE_ISO)` injeta `now` no `detectStage` e `status` para `age_days=0`; `churn_90d` deliberadamente NÃO controlado (git usa system clock, não `now` injetado) — assertado apenas como `>= 0` para sanidade. Cleanup via `afterEach` que esvazia stack `cleanups[]` para idempotência mesmo se um teste falhar mid-run. Verificação: 234/234 unit tests verdes (203 prévios + 31 novos), typecheck limpo. Anchor: cada bloco `describe` espelha 1:1 o contrato de `EXPECTED.md` da fixture correspondente — futuras mudanças em qualquer EXPECTED ou no detector quebram aqui antes do harness/e2e.

## Priority 3: Fase 2 — Setup greenfield (escrita)

- [x] Criar `cli/src/lib/fs-safe.ts` (escritas vão a `.lint-manifest.json` para uninstall completo; checa working tree limpo se `--strict`) (why: PLAN §Contratos CLI – manifest + safe-write). Implementado como utilitário puro com seam `SafeIO` para injeção de FS/git em testes. API pública: `safeWriteFile(cwd, relPath, content, opts?, io?)` (orquestra resolveSafePath → strict-check opcional → mkdir-p → writeFile → recordEntry), `loadManifest`, `recordEntry` (idempotente — re-write do mesmo path substitui entry sem duplicar), `removeEntry` (no-op se ausente), `setManifestField` (para `theme`; cria manifest se inexistente), `deleteManifest` (idempotente), `resolveSafePath`, `manifestPath`. **Validação de path** (resolveSafePath): rejeita string vazia, paths absolutos (`/etc/passwd`, `C:\Windows`), escape via `..` (`../foo`, `subdir/../../foo`), e normalização para cwd (`.`, `./`). **Manifest schema** v1: `{ version:"1", created_at, updated_at, theme?, entries: [{ path, kind, created_at, merged? }] }` com `path` em formato POSIX (split `\\` → `/`) para estabilidade cross-platform; `loadManifest` retorna `null` em arquivo ausente/malformed/version-mismatch e dropa silenciosamente entries que falham `isValidEntry` (forward-compat). **`ManifestEntryKind`** union categoriza ownership para uninstall (Phase 3): `preset|hook|husky|lintstaged|decisions|template|other` são owned-by-qualy (deletar); `settings|scripts|coverage` são merged em files pré-existentes (preservar). **Strict mode**: quando `opts.strict===true`, chama `dirtyFilesFn(cwd)` (default delega a `git.ts#dirtyFiles`) e retorna error se tree sujo OR git falha — defesa em profundidade sobre `git-clean-check` do harness. **Manifest excluído do tracking**: `safeWriteFile` detecta o próprio `.lint-manifest.json` (via `relative(cwd, abs) === MANIFEST_FILENAME`) e não chama `recordEntry` — `recorded:false` no result; previne loop infinito de auto-record. **Resiliência**: write errors viram `{ok:false, error}` ao invés de throw; mode opcional via segundo parâmetro `writeFileFn(p, c, mode?)` (suporta `0o755` para `post-edit.sh`). 32 unit tests cobrindo: 7 de resolveSafePath (empty/absolute/escape/embedded-escape/cwd-itself/normal/nested); 13 de safeWriteFile (write+record, manifest skip, POSIX normalization, idempotência re-write, merged flag, kind default, rejeição absolute/escape sem write, strict dirty/git-fail/clean, error vira result não throw); 5 de loadManifest (null absent/malformed/version-mismatch, drop entries inválidos, theme valid/non-string ignored); 5 de remove/setField/delete (remove com timestamp update, no-op missing, setField cria manifest, setField preserva entries, delete idempotente); 2 de integração FS real (mkdtempSync + parent dir creation com mode 0o755, manifest reflete múltiplas writes). Backpressure 4/4 (typecheck, lint placeholder, 266 tests, build placeholder).
- [x] Copiar in-source `cli/src/presets/oxlint/{greenfield,brownfield-moderate,legacy}.{fast,deep}.json` com thresholds da tabela do SPEC §3 (why: SPEC §3 calibração; presets versionados). Implementados 6 arquivos JSON em `cli/src/presets/oxlint/` (greenfield/brownfield-moderate/legacy × fast/deep). Convenção SPEC §4: cada preset declara `$schema:"./node_modules/oxlint/configuration_schema.json"` (path padrão de `oxlint init`, copiado byte-a-byte para projeto-alvo) e `_comment:"qualy preset · stage=<x> · tier=<y> · generated=2026-05-03"`. **Fast tier** (sem `quality-metrics`): só `categories.{correctness,suspicious}` — greenfield/brownfield mantêm `correctness:error` (estrito), legacy afrouxa para `correctness:warn` (não bloqueia). **Deep tier** acrescenta `plugins:["quality-metrics"]` + 6 rules com options `{max:N}` espelhando 1:1 a tabela do SPEC §3: `quality-metrics/wmc`, `halstead-volume`, `halstead-effort`, `lcom`, `cbo`, `dit`. Severidades: greenfield {wmc:error, halstead-volume:warn, halstead-effort:warn, lcom:warn, cbo:error, dit:warn} max {15,800,300,0,8,4}; brownfield-moderate idem severities, max {20,1000,400,2,10,5}; legacy todas warn (não bloquear refactor em legado), max {40,2000,1000,4,20,6}. Halstead modelado como 2 rules separadas (volume + effort) seguindo a tabela; LCOM/CBO/DIT/WMC com `{max}` consistente com `rules_active` exemplificado em SPEC §3 audit JSON. Suite de contrato em `cli/tests/unit/presets-oxlint.test.ts` (39 testes): trava metadata (`$schema`, `_comment` com stage+tier corretos) em todos os 6 arquivos; assert que fast tier não carrega rules `quality-metrics/*` nem plugin; matriz exata stage×rule de severidade+max para deep tier (lockando a tabela do SPEC §3 — drift quebra aqui antes de chegar no usuário); fast tier severities (greenfield/brownfield error, legacy warn) explícitas. Verificação: 305/305 unit tests verdes (266 prévios + 39 novos), typecheck limpo. Backpressure 4/4 (typecheck, lint placeholder, tests, build placeholder).
- [x] Copiar in-source `cli/src/presets/coverage/{vitest.greenfield.ts,vitest.brownfield.ts,vitest.legacy.ts,jest.greenfield.json,jest.brownfield.json,jest.legacy.json}` (why: SPEC §3 estratégia coverage). Implementados 6 arquivos em `cli/src/presets/coverage/` espelhando a tabela do SPEC §3 (Estratégia de coverage). Vitest `.ts` exporta `default { test: { coverage: { provider:"v8", reporter, thresholds } } } as const` — typed const sem `defineConfig` (evita acoplamento ao runtime de vitest no cli typecheck) mas shape-compatible com vitest config para merge via ts-morph em `install-coverage`. Header docstring carrega tag `runner=vitest · stage=<x>` para parsing fácil. Jest `.json` carrega `coverageProvider:"v8"` + `collectCoverage:true` + `coverageReporters:["text","json","json-summary","html"]` + `coverageThreshold.global.{lines,functions,branches,statements}` (schema oficial jest), com `_comment` para metadata. Thresholds 1:1 com a tabela: greenfield {90,90,80,90}, brownfield {70,70,60,70}, legacy {40,40,30,40}. **Warn-only marker** (`_warnOnly:true`) presente apenas em legacy (ambos runners) — semantica explicita: vitest/jest não têm warn nativo em coverage threshold, então `install-coverage` deverá decidir entre (a) escrever os valores e tolerar exit code de fail, ou (b) emitir wrapper soft-check; ambos os caminhos lêem o flag `_warnOnly`. Suite de contrato `cli/tests/unit/presets-coverage.test.ts` (51 testes) trava: presença + parsing dos 6 arquivos, header tags em vitest .ts (`stage=` + `runner=vitest`), shape `test.coverage.thresholds`, provider `v8` em ambos runners, `collectCoverage` em jest, **matriz exata stage × {lines,functions,branches,statements}** para ambos (drift quebra aqui antes do usuário), warn-only flag em legacy + ausência em greenfield/brownfield, e cross-runner consistency (vitest thresholds === jest coverageThreshold.global por estágio — proteção contra divergência futura entre os dois runners). Verificação: 356/356 unit tests verdes (305 prévios + 51 novos). Backpressure 4/4 (typecheck, lint placeholder, tests, build placeholder).
- [x] Criar template `cli/src/templates/post-edit.sh` com `set -euo pipefail` filtrando `$CLAUDE_FILE_PATHS` (why: SPEC §4 templates copiados). Implementado em `cli/src/templates/post-edit.sh` (mode 0o755) com contrato fiel ao SPEC §4: shebang `#!/usr/bin/env bash`, `set -euo pipefail`, leitura defensiva `${CLAUDE_FILE_PATHS:-}` (no-op `exit 0` quando ausente/vazio), filtro case-statement com union `*.ts|*.tsx|*.js|*.jsx` (apenas as 4 extensões do oxc; explicitamente sem `.mjs/.cjs/.mts/.cts/.vue/.svelte`), `exit 0` quando filtro descarta todos os paths, resolução de oxlint preferindo `./node_modules/.bin/oxlint` (instala-local) sobre `command -v oxlint` (global) — ambos usam `--config oxlint.fast.json` (PostToolUse fica no fast tier; deep é wired via lint-staged no pre-commit). Quando oxlint não está disponível: stderr `qualy/post-edit: oxlint not found; skipping` + `exit 0` (princípio: hook nunca quebra o loop do agente). Suíte de contrato `cli/tests/unit/templates-post-edit.test.ts` com 13 testes em dois `describe` blocks: **Static contract (8)** trava shebang exato, presença de `set -euo pipefail`, leitura `${CLAUDE_FILE_PATHS:-}`, filtro de extensões positive+negative (4 incluídas + 5 explicitamente excluídas: py/rs/go/vue/svelte), referência a `oxlint.fast.json` + ausência de `oxlint.deep.json`, ordem local-before-global (assert via `indexOf`), warning string + exit 0, executable bit `(mode & 0o100) === 0o100`. **Runtime quiet-exit paths (5)**: `spawnSync('bash', [TEMPLATE_PATH], …)` em tmp dir isolado via `mkdtempSync` — (i) CLAUDE_FILE_PATHS unset → exit 0 stdout/stderr vazios, (ii) CLAUDE_FILE_PATHS empty → idem, (iii) só extensões não-suportadas (`.py .md .rb`) → idem, (iv) extensões suportadas + sem oxlint no PATH (`/usr/bin:/bin`) e sem `node_modules/.bin/` → exit 0 com warning em stderr, (v) stub `node_modules/.bin/oxlint` (script bash que `printf` os args para um log) → exit 0, args recebidos são exatamente `["--config","oxlint.fast.json","/a/keep.ts","/c/keep.jsx","/e/keep.tsx","/f/keep.js"]` (preserva ordem original do filtro, descarta `.py`/`.md`). Cleanup via `afterEach` que esvazia stack `cleanups[]` (idempotente). Backpressure 4/4: typecheck limpo, lint placeholder ok, 369/369 tests verdes (356 prévios + 13 novos), build placeholder ok.
- [x] Criar template `cli/src/templates/lintstagedrc.example.js` (ES modules, fast antes de deep) (why: SPEC §4 templates). Implementado em `cli/src/templates/lintstagedrc.example.js` — ES module (`export default`) mapeando o glob `*.{ts,tsx,js,jsx}` (apenas as 4 extensões oxc) para o pipeline pre-commit em três passos na ordem fixa: (i) `oxfmt --write` (formata in-place antes de qualquer validação), (ii) `oxlint --config oxlint.fast.json` (correctness/suspicious — barato, falha rápido), (iii) `oxlint --config oxlint.deep.json` (quality-metrics + ts-morph — caro, só roda em sobreviventes do fast). Ordering invariant `fast antes de deep` (SPEC §4 line 314) é o ponto inteiro do template: o tier deep só carrega `quality-metrics`/`ts-morph` em arquivos que já passaram no fast tier, evitando o pior caso de pagar o custo deep para depois falhar em uma rule barata. lint-staged appendará paths staged a cada comando — sem usar function form (mantém o config legível e diff-friendly). Header docstring documenta requisito `"type": "module"` no `package.json` do projeto e fallback `.lintstagedrc.mjs` para projetos CommonJS — nota para o `install-husky` futuro decidir o sufixo correto na cópia. Suite de contrato `cli/tests/unit/templates-lintstagedrc.test.ts` (6 testes em dois `describe` blocks): **Static contract (5)** trava (i) ESM via regex `^export default \{` + ausência de `module.exports`/`require(`, (ii) glob anchorado — usa `text.match(/"\*\.\{[^"]+\}"/g)` para extrair só os globs do código (header comments podem mencionar `.mjs` legitimamente, então não dá para asserir `not.toMatch(/\.mjs/)` no body inteiro), assertando `[\"*.{ts,tsx,js,jsx}\"]` exato — 1 só glob, 4 só extensões, (iii) presença de ambos `oxlint.fast.json` e `oxlint.deep.json`, (iv) `indexOf` ordering fast<deep, (v) `indexOf` ordering oxfmt<fast. **Runtime ESM shape (1)**: `await import(pathToFileURL(TEMPLATE_PATH).href)` — valida que o arquivo é um ESM válido (não só sintaxe regex-checada), `default` é objeto não-null, único key é o glob esperado, value é Array com os 3 comandos na ordem exata. Erro inicial pego pelo backpressure: o teste anterior `not.toMatch(/\.mjs/)` falhou porque o header comment menciona `.mjs` como fallback CommonJS — corrigido escopando a asserção via `text.match(...glob regex...)` em vez do body inteiro (lição: assertar features do contrato no shape correto, não substring-blanket). Backpressure 4/4: typecheck limpo, lint placeholder ok, **375/375 tests verdes** (369 prévios + 6 novos), build placeholder ok. Verificação adicional: `tsconfig.json` raiz (`include:["cli/src/**/*.ts"]`) e `cli/tsconfig.json` ignoram `.js`, então o template não entra no typecheck nem afeta vitest `include:["cli/tests/unit/**/*.test.ts"]`.
- [x] Criar template `cli/src/templates/lint-decisions.md.tpl` (registro append-only com data, rule, motivo, autor) (why: SPEC §4 + §6 Always). Implementado em `cli/src/templates/lint-decisions.md.tpl` como skeleton estático copiado byte-a-byte para `<target>/docs/lint-decisions.md` na fase de install. Estrutura: H1 `# Lint decisions` + intent paragraph (commands cobertos: `/lint:rules:add`, `/lint:rules:remove`, `/lint:update`, `/lint:setup` lower coverage) + seção `## Conventions` (append-only, ISO-8601 UTC `YYYY-MM-DDTHH:MM:SSZ`, autor via `git config user.email`, reason via `AskUserQuestion` — required em loosening, optional em tightening) + seção `## Entry shape` (template H3 indented code block) + seção `## Entries` com par de markers HTML `<!-- qualy:entries-start -->` / `<!-- qualy:entries-end -->`. Decisão de design: markers HTML em vez de fenced code block ou seção H3 — invisíveis na renderização mas fáceis de localizar deterministicamente por `indexOf` no append do CLI; região entre markers fica vazia (whitespace only) na instalação para que o primeiro append não tenha precedente. Kinds enumerados (`rule-add | rule-remove | threshold-raise | threshold-lower | coverage-lower | rec-apply`) cobrem todos os eventos que SPEC §6 requer logging (linhas 389, 423, 462) — install commands de Phase 5 importarão essa enum. Suite `cli/tests/unit/templates-lint-decisions.test.ts` (15 testes) trava: header H1 + append-only intent + 4 fields obrigatórios do SPEC §4 (timestamp/author/reason/rule); ISO-8601 e `git config user.email` e `AskUserQuestion` como anchors; markers exatamente uma vez cada, em ordem start→end, com região entre eles vazia, e ambos abaixo do `## Entries` heading; enumeração completa dos 6 kinds; H3 template literal `### <timestamp> — <kind>: <subject>`; file hygiene (UTF-8 sem BOM, LF, trailing newline). Backpressure 4/4: typecheck limpo, lint placeholder ok, **390/390** tests verdes (375 prévios + 15 novos), build placeholder ok.
- [x] Criar template `cli/src/templates/package-scripts.json` com `lint`, `lint:deep`, `format`, `coverage` (why: SPEC §7.1 acceptance scripts). Implementado como JSON snippet estático em `cli/src/templates/package-scripts.json` consumido por `install-scripts.ts` (Phase 2) para mesclar em `<target>/package.json#scripts`. Shape top-level: `{ _comment, scripts, coverage_by_runner }`. **`scripts`** declara as 3 entradas runner-agnósticas que SPEC §7.1 lista por nome: `lint:"oxlint --config oxlint.fast.json ."`, `lint:deep:"oxlint --config oxlint.deep.json ."`, `format:"oxfmt --write ."` — paths apontam para os presets emitidos por `install-oxlint` (`oxlint.fast.json`/`oxlint.deep.json`) e ao formatter da família oxc (`oxfmt`); todos usam `.` (project-wide) já que `npm run lint` deve rodar contra o projeto inteiro (vs. lint-staged que recebe paths via stdin no pre-commit). **`coverage_by_runner`** mapeia `vitest:"vitest run --coverage"` e `jest:"jest --coverage"` — install-scripts seleciona uma das duas a partir do output de `detect-test-runner` e escreve em `scripts.coverage`; quando runner é `none`, install-scripts deve omitir a entrada `coverage` (template não declara fallback `none`/`default` propositalmente — força a decisão no caller). Decisão de design: NÃO emitir `coverage` direto em `scripts` (como os outros 3) porque o valor depende de detecção runtime; isolar em `coverage_by_runner` deixa `scripts` byte-merge-safe e evita placeholders tipo `<runner-coverage-cmd>` que confundiriam parser JSON. Suite de contrato `cli/tests/unit/templates-package-scripts.test.ts` (16 testes em 4 `describe` blocks): **file hygiene (3)** — JSON válido, LF + trailing newline, sem BOM; **top-level shape (4)** — exatos 3 keys top-level, `_comment` referencia "qualy"+"SPEC §7.1"+"install-scripts" para provenance, `scripts` e `coverage_by_runner` são objetos não-vazios de strings; **scripts contract (5)** — exatamente {format, lint, lint:deep} (lock contra entrada extra), `lint`/`lint:deep` invocam oxlint com preset correto e NÃO referenciam o outro preset (cross-contamination guard), `format` invoca `oxfmt --write`, `scripts` NÃO declara `coverage` estaticamente; **coverage_by_runner contract (4)** — exatos {vitest, jest}, valores literais (`vitest run --coverage` / `jest --coverage`), ausência de `none`/`default` (força install-scripts a tratar runner ausente). Backpressure 4/4: typecheck limpo, lint placeholder, **406/406 tests verdes** (390 prévios + 16 novos), build placeholder ok.
- [x] Implementar `cli/src/commands/install/oxlint.ts` (escreve `oxlint.fast.json` e `oxlint.deep.json` a partir do preset do estágio) (why: PLAN §Contratos CLI). Implementado como `installOxlint(opts, deps?)` puro + handler `runInstallOxlint(argv)`. Resolve estágio em duas vias: `--stage <name>` explícito (validado contra `["greenfield","brownfield-moderate","legacy"]`) ou via `detectStage({cwd})` quando flag ausente; `stageSource: "explicit"|"detected"` é emitido para auditoria. Lê os 2 presets bundled em `cli/src/presets/oxlint/<stage>.<tier>.json` (path resolvido via `import.meta.url` + `..` para que funcione em copy/symlink/install global, conforme ADR 0009) e escreve byte-a-byte para `<cwd>/oxlint.fast.json` e `<cwd>/oxlint.deep.json` via `safeWriteFile` com `kind:"preset"` — manifest registra ambos paths para uninstall. `--strict` propaga para `safeWriteFile`, retornando `DIRTY_TREE` (3) quando o working tree está sujo (detectado via reason substring "working tree is dirty" no result.error). Idempotente: re-rodar com mesmo estágio sobrescreve bytes idênticos e `recordEntry` deduplica por path; trocar de estágio sobrescreve em place. Exit codes: `OK`(0) sucesso, `USAGE_ERROR`(4) flag inválida ou stage desconhecido, `RECOVERABLE_ERROR`(1) detect-stage fail / preset read fail / write fail genérica, `DIRTY_TREE`(3) `--strict` + tree sujo. Wired no dispatcher via `HANDLER_OVERRIDES`. Suite `cli/tests/unit/install-oxlint.test.ts` (18 testes em 5 `describe` blocks): contrato byte-a-byte para 3 estágios × {fast,deep} (6 tests), manifest entries com `kind:"preset"` para os 3 estágios (3 tests), delegação a detectStage + falha surfaced como `stage_detection_failed` (2), error paths `preset_read_failed` (read throw → nada escrito) e `write_failed` em strict+dirty (2), idempotência (re-run não duplica entries; switch de estágio sobrescreve em place — 2), parser de flags (defaults, --cwd/--stage/--strict, stage inválido, missing values, unknown flag, --help/-h — 6). Smoke test contra tmp repo `/tmp/qualy-smoke-oxlint`: `install-oxlint --cwd ... --stage greenfield` → stdout `{ok:true, written:[{path:"oxlint.fast.json",tier:"fast",bytes:230},{path:"oxlint.deep.json",tier:"deep",bytes:619}]}`, files presentes byte-iguais aos presets bundled, `.lint-manifest.json` v1 com 2 entries kind=preset. Backpressure 4/4: typecheck limpo, lint placeholder, **424/424 tests verdes** (406 prévios + 18 novos), build placeholder.
- [x] Implementar `cli/src/commands/install/hook.ts` (merge em `.claude/settings.json` + escreve `.claude/hooks/post-edit.sh`) (why: SPEC §1.4 hook PostToolUse; SPEC §6 Ask first – merge se já existe). Implementado como `installHook(opts, deps?)` puro + handler `runInstallHook(argv)` + função `mergeSettings(existing)` exposta para testes. Em duas etapas: (i) copia byte-a-byte o template `cli/src/templates/post-edit.sh` (resolvido via `import.meta.url`) para `<cwd>/.claude/hooks/post-edit.sh` com mode `0o755` via `safeWriteFile` kind=`hook`; (ii) merge não-destrutivo em `<cwd>/.claude/settings.json` produzindo `hooks.PostToolUse[]` com matcher `Write|Edit|MultiEdit` apontando para `.claude/hooks/post-edit.sh` (substring match para detecção idempotente — robusto a paths `./...` ou variações de escrita). Três modos para o settings: **created** (não existia), **updated** (existia + outras entries; preserva tudo, anexa nossa entry), **unchanged** (entry já referencia `post-edit.sh` — não rewrite). Manifest: kind=`settings`, `merged:true` (uninstall não pode deletar arquivo do usuário). **Malformed settings.json** aborta com `settings_malformed` sem overwrite (script já foi escrito antes; uninstall limpa). `--strict` propaga para `safeWriteFile` (DIRTY_TREE). Output: `{ ok, cwd, script:{path,bytes,recorded}, settings:{path, action, recorded} }`. Exit codes: OK, USAGE_ERROR, RECOVERABLE_ERROR, DIRTY_TREE. Wired no dispatcher via HANDLER_OVERRIDES. **Suite `cli/tests/unit/install-hook.test.ts` (30 testes em 5 describe blocks)**: fresh project (3 — byte-exact script copy, fresh settings.json com PostToolUse entry, manifest entries com kinds e merged flag corretos), merge into existing (5 — append preservando env+SessionStart+entries diferentes, action=unchanged em entry idêntica, recognize matcher=Write referenciando nosso script, rejeição de malformed sem clobber, rejeição de root array), error paths (2 — template_read_failed sem clobber, write_failed em strict+dirty), idempotency (1 — single manifest entry per path), pure mergeSettings (6 — created/updated/unchanged + missing hooks key + parse error + non-object root), parser de flags (5). Smoke test contra tmp repo: 1ª run → action="created" + script byte-exact + settings.json com matcher Write|Edit|MultiEdit + 2 manifest entries; 2ª run → action="unchanged", recorded=false. Backpressure 4/4: typecheck limpo, lint placeholder, **446/446 tests verdes** (416 prévios + 30 novos), build placeholder.
- [x] Implementar `cli/src/commands/install/scripts.ts` (merge idempotente em `package.json#scripts`) (why: PLAN §Contratos CLI – idempotência). Implementado como `installScripts(opts, deps?)` puro + handler `runInstallScripts(argv)` + funções expostas `resolveDesiredScripts()` e `mergeScripts()`. **Resolução do payload desejado**: por padrão lê `cli/src/templates/package-scripts.json` (resolvido via `import.meta.url` para funcionar em copy/symlink/install global) e seleciona `coverage` a partir de `coverage_by_runner[runner]` quando `runner ∈ {vitest, jest}`; com `runner === "none"` o trio (lint, lint:deep, format) é o payload completo (sem coverage). Flag `--scripts <json>` (PLAN §Contratos CLI) override completo, ignora o template — usado por testes e por harness avançado. **Merge three-way não-destrutivo** contra `package.json#scripts`: chave ausente → `added`, chave presente com valor idêntico → `skipped` (idempotência), chave presente com valor diferente → `conflicts: [{name, existing, proposed}]` SEM overwrite (harness decide via `AskUserQuestion`; CLI nunca pergunta — PLAN §3). Outros campos do `package.json` (name, version, type, dependencies, devDependencies, etc.) preservados intactos. **Idempotência**: quando `added.length === 0`, NÃO reescreve o arquivo (action="noop", recorded=false) — evita drift de mtime e manifest. Apenas action="updated" chama `safeWriteFile` com `kind:"scripts"`, `merged:true` (uninstall não pode deletar `package.json`; Phase 3 fará remoção surgical de chaves). **Defesas**: `package.json` ausente → `package_json_missing`; malformed/non-object → `package_json_malformed`; root template malformed → `template_malformed`; runner=vitest/jest mas `coverage_by_runner.<runner>` ausente → erro template; `scripts` field não-objeto no package.json → tratado como `{}` (defensivo, permite criar do zero). **`--strict`** propaga para `safeWriteFile` (DIRTY_TREE). Output `{ok, cwd, path, added[], skipped[], conflicts[], recorded, action}`. Exit codes: OK em sucesso (conflicts NÃO falham — informacionais), USAGE_ERROR em flag inválida ou JSON malformado, RECOVERABLE_ERROR em read/parse/write fail, DIRTY_TREE em strict+dirty. **Wired no dispatcher** via `HANDLER_OVERRIDES`. Suite `cli/tests/unit/install-scripts.test.ts` (38 testes em 7 describe blocks): **resolveDesiredScripts** (6) — vitest/jest/none + override + template malformed; **mergeScripts** (6) — added/skipped/conflicts classification, preserva chaves não relacionadas, non-string existing surface as conflict, insertion order preservada; **fresh merge greenfield** (5) — vitest writes 4 scripts e preserva name/version, runner=none omits coverage, runner=jest variant, manifest kind=scripts merged=true, type/deps/devDeps preservados; **idempotency + conflicts** (3) — segunda run é noop (bytes idênticos), conflicts não overwrite (lint=eslint . --fix preservado), todos skipped → noop; **error paths** (6) — package.json missing, malformed (`{ not json`), non-object root (`[]`), template missing em default mode, override mode bypassa template, strict+dirty → write_failed contendo "working tree is dirty"; **package.json sem scripts** (2) — cria scripts field, scripts="not an object" tratado como `{}`; **parseInstallScriptsArgs** (10) — defaults, --cwd/--runner/--strict combinados, --scripts JSON parsing, runner inválido, JSON malformado, valores não-string, array como --scripts, missing values, --zonk, --help/-h. **Smoke test contra tmp dir**: `package.json` com `scripts:{build:"tsc"}` + `install-scripts --runner vitest` → 1ª run adiciona 4 keys + `build:"tsc"` preservado + manifest `kind:"scripts" merged:true`; 2ª run idêntica → action="noop", added=[], skipped=[lint,lint:deep,format,coverage], recorded=false (zero bytes diff). Backpressure 4/4: typecheck limpo, lint placeholder, **484/484 tests verdes** (446 prévios + 38 novos), build placeholder.
- [x] Implementar `cli/src/commands/install/deps.ts` (chama `npm/pnpm/yarn/bun add` conforme `pkg-manager.ts`; instala oxlint, oxfmt, quality-metrics, ts-morph quando deep) (why: PLAN §Contratos CLI – install-deps). Implementado como `installDeps(opts, deps?)` puro + handler `runInstallDeps(argv)` + helpers expostos `specName()`, `buildArgs()`, `DEFAULT_DEPS`. **Algoritmo**: (i) resolve set desejado — `--deps <json>` (array de strings `"name"` ou `"name@version"`) override outright, senão `DEFAULT_DEPS = ["oxlint","oxfmt","quality-metrics","ts-morph"]` (espelha SPEC §1.18); (ii) lê `<cwd>/package.json` e computa idempotência: nomes presentes em `dependencies` OU `devDependencies` viram `skipped` (preserva escolha do usuário se moveu para prod deps); (iii) se nada a instalar → `action:"noop"`, **sem subprocess, sem manifest write** (zero side effects); (iv) detecta package manager via `detectPackageManager()` (lockfile-driven); (v) shell-out via `execFileSync` (no shell — nunca expande globs por `/bin/sh`); (vi) on success grava 1 manifest entry per pacote com path virtual `package.json#devDependencies/<name>`, kind=`dep` (novo, adicionado a `ManifestEntryKind` em `fs-safe.ts`), merged=true (uninstall fará `pkg-manager remove` da lista). **Argv shape per manager**: npm `install --save-dev`, pnpm `add --save-dev`, yarn `add --dev`, bun `add --dev` — todos adicionam a `devDependencies`. **`specName()`** extrai nome bare de specs com versão (`oxlint@1.0.0` → `oxlint`) e scoped (`@scope/name@2.0.0` → `@scope/name`) para matching correto contra package.json. **Defesas**: package.json missing/malformed/non-object aborta sem subprocess; deps array vazio ou com não-string vira USAGE_ERROR; subprocess non-zero vira `pkg_install_failed` com stderr capturado (parcial: package.json pode ter sido modificado pelo pm — não roll back); strict+dirty checa via `dirtyFiles()` antes de subprocess (DIRTY_TREE); strict+git fail vira `git_check_failed`. **Wired no dispatcher** via `HANDLER_OVERRIDES` Map. **Suite `cli/tests/unit/install-deps.test.ts` (40 testes em 7 describe blocks)**: `specName` (4 — bare, version, scoped sem version, scoped com version, empty), `buildArgs` (5 — npm/pnpm/yarn/bun shapes + ordering deterministico), `DEFAULT_DEPS` (1 — quartet em ordem), fresh greenfield (5 — DEFAULT_DEPS via npm, pnpm/bun/yarn shape via fakeDetect, manifest entries com kind=dep merged=true e timestamp fixo), idempotency (4 — skip dev devDeps, treat prod deps como already-installed, noop quando todos presentes, version-suffixed specs matched por bare name), error paths (8 — package_json_missing/malformed/non-object, pkg_install_failed propaga stderr e impede manifest write, deps_empty/invalid via USAGE_ERROR shape, dirty_tree e git_check_failed sem subprocess), override mode (1), parser de flags (12 — defaults, --cwd/--strict, --deps válidos com ou sem versão, malformed JSON, non-array, non-string, empty string, missing values, --help/-h, unknown). **Smoke test contra tmp dir real**: 1ª run com `["oxlint","oxfmt"]` em pkg com `oxlint` em devDependencies → `installed:["oxfmt"]`, `skipped:["oxlint"]`, npm install ocorre, package-lock.json criado, `node_modules/` populado, manifest com 1 entry kind=dep para oxfmt; 2ª run idempotente → `action:"noop"`, source muda de `default` para `package-lock.json`. Backpressure 4/4: typecheck limpo, lint placeholder, **524/524 tests verdes** (484 prévios + 40 novos), build placeholder.
- [x] Implementar `cli/src/commands/install/husky.ts` (cria `.husky/pre-commit` + `.lintstagedrc.js`) (why: SPEC §1.4 lint-staged + husky). Implementado como `installHusky(opts, deps?)` puro + handler `runInstallHusky(argv)` + helpers expostos `chooseLintstagedFilename(packageJsonRaw)` e `detectExistingLintstaged(cwd, existsFn, readFileFn)`. **Dois artefatos decididos independentemente, ambos resilientes a estado prévio do usuário (SPEC §6 "Ask first" — CLI nunca clobbera, harness orquestra)**: (A) `.husky/pre-commit` (mode 0o755) — absent → escreve script mínimo `#!/usr/bin/env sh\nnpx lint-staged\n` (action="created"); presente E contém "lint-staged" → action="unchanged" (no rewrite, recorded=false); presente sem reference → action="kept" (preserva bytes, recorded=false). Husky v9 dispensa boilerplate de `_/husky.sh`. `npx lint-staged` (vs `lint-staged` bare) garante resolução cross-pm (npm/pnpm/yarn/bun). (B) lint-staged config — sonda 10 nomes (`.lintstagedrc`, `.lintstagedrc.{json,yaml,yml,js,cjs,mjs}`, `lint-staged.config.{js,cjs,mjs}`) na ordem de `LINT_STAGED_CONFIG_FILES` (mesma de `status.ts` para consistência) + `package.json#lint-staged` inline; QUALQUER hit → action="kept" com path do sobrevivente; nada presente → copia byte-a-byte `cli/src/templates/lintstagedrc.example.js`. **Filename selection**: `.lintstagedrc.js` quando `package.json#type === "module"`, senão `.lintstagedrc.mjs` (`.mjs` é unconditionally ESM regardless de `type`, então é o default seguro quando package.json ausente/malformed/sem type — alinha com fallback documentado no template). Manifest kinds: `husky` para pre-commit, `lintstaged` para o config; **nenhum carrega `merged: true`** (qualy é dono completo de ambos os arquivos quando os escreve — uninstall pode deletar). `--strict` propaga para `safeWriteFile`; falha em strict+dirty surface como `write_failed` com reason "working tree is dirty" (mapeada para `DIRTY_TREE` no handler). Output: `{ ok, cwd, husky:{path,action,recorded}, lintstaged:{path,action,recorded} }`. Exit codes: OK em sucesso, USAGE_ERROR em flag inválida, RECOVERABLE_ERROR em template_read_failed/write_failed, DIRTY_TREE em strict+dirty. Wired no dispatcher via `HANDLER_OVERRIDES`. **Suite `cli/tests/unit/install-husky.test.ts` (32 testes em 8 describe blocks)**: fresh project (5 — pre-commit body+mode, .mjs default sem package.json, .js com type=module, fallback .mjs em pkg malformed, manifest kinds husky/lintstaged sem merged); pre-existing pre-commit (2 — unchanged quando contém lint-staged via `yarn lint-staged --concurrent false` substring, kept quando contém só `npm test`); pre-existing lint-staged config (4 — `.lintstagedrc.json` byte-preserved, `lint-staged.config.cjs` detectado, `package.json#lint-staged` inline mesmo com type=module, file-based vence inline em conflito); error paths (3 — template_read_failed sem clobber do husky já escrito, write_failed em strict+dirty, lintstaged não escrito quando husky falha em strict+dirty); idempotency (2 — segunda run idêntica ao manifest, action=unchanged/kept); `chooseLintstagedFilename` (6 — null/parse fail/array root/missing type/type=commonjs/type=module); `detectExistingLintstaged` (5 — empty, primeiro file, ordem de probe, inline package.json, ignora malformed); `parseInstallHuskyArgs` (5 — defaults, --cwd/--strict, missing --cwd, unknown flag, --help/-h). Backpressure 4/4: typecheck limpo, lint placeholder, **556/556 tests verdes** (524 prévios + 32 novos), build placeholder.
- [x] Criar `cli/src/lib/ts-config-edit.ts` editando `vitest.config.ts` via ts-morph preservando comentários (why: PLAN §Critical files – único caminho confiável p/ TS configs). Implementado como módulo puro (sem FS) que recebe source text e devolve source text — composável com `safeWriteFile`. **API pública narrow**: `parseConfigSource(text)` (in-memory ts-morph project, nunca throw — malformed text vira `{ok:false}` mas ts-morph é defensivo então a maioria dos garbages parsa); `findDefaultExportObject(sourceFile)` (locate `export default {…}` ou `export default fn({…})` desembrulhando `CallExpression.arguments[0]`, `as const`, `satisfies T`, parens — retorna `null` quando default é identifier ou ausente); `readObjectPath(obj, path)` (lê leaf como JS value: string/number/boolean/null/array/object recursivo; suporta unary minus em numbers; retorna `undefined` se segmento ausente, é shorthand/spread/method, ou leaf é não-literal); `setObjectPath(obj, path, valueText)` (set/replace por path; cria intermediários como `{}` quando ausentes; erra se intermediário existe mas não é object literal); `serializeValue(value)` (JS value → TS expression text; identifier keys unquoted, demais via `JSON.stringify`; throws em `Infinity`/`NaN`/function/symbol). **High-level helpers**: `readVitestThresholds(text)` (retorna `{lines?,functions?,branches?,statements?}|null` lendo `test.coverage.thresholds`; só keys numéricas presentes — string/missing são droppadas; null se nenhuma das 4 conhecidas) e `applyVitestCoverage(text, patch)` que merge **leaf-by-leaf** (provider, reporter, thresholds.{lines,functions,branches,statements}) sob `test.coverage` — `changed:false` quando todos os leaves já carregam o valor desejado (deepEqual com sort de keys + comparação element-wise de arrays); preserva chaves não declaradas no patch (ex: `coverage.exclude`, `setupFiles` em `test`); funciona com ou sem wrapper `defineConfig()`. Constante exportada `VITEST_THRESHOLD_KEYS = ["lines","functions","branches","statements"] as const` lockando a tabela do SPEC §3. **Comment preservation**: ts-morph `addPropertyAssignment` e `setInitializer` mantêm trivia (banner comments, inline notes em siblings) — verificado em smoke test contra `// keep me` e `// existing inline note`. Suite `cli/tests/unit/ts-config-edit.test.ts` (**57 tests** em 8 describe blocks): parseConfigSource (2 — valid TS, garbage não throw); findDefaultExportObject (8 — direct object, defineConfig wrapper, `as const`, parens, no default, identifier default, no-args call, non-object first arg); readObjectPath (12 — string/number/boolean/null/negative/template/nested/array/object/missing/non-object intermediate/non-literal/shorthand/spread); setObjectPath (7 — add missing, replace existing, create intermediates, merge into existing intermediate, empty path rejected, non-object intermediate errors, function-call intermediate errors); serializeValue (8 — primitives, string escapes, arrays, identifier vs non-identifier keys, nested, empty obj, non-finite, function); readVitestThresholds (7 — no default, no thresholds, only unknown keys, full set, subset, ignore non-numeric, defineConfig wrapper); applyVitestCoverage (10 — fresh empty, merge preserving siblings/exclude, comment preservation, no-op idempotency, reporter element diff vs identity, only patched leaves change, defineConfig wrapper, no-default error, non-object intermediate error, threshold keys constant); integration (2 — round-trip applyVitestCoverage→readVitestThresholds, idempotency em segunda call). Backpressure 4/4: typecheck limpo, lint placeholder, **613/613 tests verdes** (556 prévios + 57 novos), build placeholder.
- [x] Implementar `cli/src/commands/install/coverage.ts` (edita `vitest.config.ts` via ts-morph ou `jest.config.*` via JSON; respeita thresholds passados via `--thresholds`; runner=none → noop; jest JS/TS configs retornam erro `jest_js_config_unsupported`; merge em `package.json#jest` quando não há config dedicado; 23 testes) (why: SPEC §3 + §7.3 acceptance)
- [x] Criar `skills/lint/SKILL.md` (router conversacional ≤200 linhas, define `QUALY_CLI` env var pattern do PLAN §Resolução do CLI) (why: SPEC §4 SKILL.md frontmatter + tamanho). Implementado em `skills/lint/SKILL.md` (90 linhas, ~57% do teto de 200 do SPEC §4 line 296). YAML frontmatter com 3 campos: `name: lint`, `description` começando por "Use when…" (SPEC §4 line 294 — ativação na primeira posição) listando os slash commands `/lint:setup`/`audit`/`update`/`status`/`report`/`rules:*`/`uninstall`/`rollback` para ajudar o discovery do modelo, `allowed-tools: Bash, AskUserQuestion, SlashCommand, Read` (mínimo necessário do router: `Bash` para o preâmbulo `QUALY_CLI`, `AskUserQuestion` para confirmações, `SlashCommand` para delegar, `Read` para auditar `.lint-audit/<ts>.json`). Corpo organizado nas 6 seções fixas do SPEC §4 line 295 em ordem: **Visão Geral** (resumo da skill + divisão harness/CLI), **Quando usar** (tabela comando → intenção), **Quando NÃO usar** (stacks bloqueadas, fora-de-escopo, ausência de Claude Code), **Resolução do CLI** (preâmbulo bash canônico do PLAN §190–198 com `QUALY_CLI="${CLAUDE_PLUGIN_ROOT:-$HOME/.claude}/skills/lint/cli/src/index.ts"` + `node --experimental-strip-types "$QUALY_CLI"`, contrato stdout/stderr/exit codes do `lib/exit-codes.ts` com mapeamento user-friendly de exit 2/3/5), **Fluxo** (4 passos: identificar intenção → AskUserQuestion se ambíguo → SlashCommand → exceção barata `qualy status` em `/lint` sem args), **Convenções globais** (uma pergunta por vez, working tree limpo, backup nomeado, `lint-decisions.md` append-only, CLI nunca pergunta), **Trade-offs** (determinismo>flex, oxc-only, sem build step, subagents finos com exceção do `lint-auditor`), **Verificação** (smoke por iteração + e2e). Cross-refs explícitas a ADRs 0006/0007/0008/0009 e às specs `.harn/docs/mvp/{SPEC,PLAN}.md`. Suite `cli/tests/unit/skill-lint-md.test.ts` (33 testes em 6 describe blocks): **file hygiene (4)** — LF only, single trailing newline, sem BOM, ≤200 linhas; **YAML frontmatter (6)** — começa com `---`, `name: lint` exato, description com prefix `^(Use when|Triggered by)` (anchor da SPEC §4 line 294), description menciona `/lint:setup`, `allowed-tools` declarado e contém `Bash` + `AskUserQuestion`; **required sections (7)** — H2 exato para Visão Geral / Quando usar / Quando NÃO usar / Fluxo / Trade-offs / Verificação + assertion de ordem via `indexOf` monotônico (evita reordering); **Resolução do CLI preamble (4)** — regex exato do `QUALY_CLI=` literal do PLAN line 196, `node --experimental-strip-types "$QUALY_CLI"` literal do PLAN line 198, menção de stdout+stderr (PLAN §Princípios line 47), exit codes `0`/`2`/`3` em backticks (matching `lib/exit-codes.ts`); **routing surface (8)** — 7 slash commands top-level (`/lint:setup`/`audit`/`update`/`report`/`status`/`uninstall`/`rollback`) + 4 subcomandos rules (`list`/`add`/`remove`/`explain`); **global conventions SPEC §6 (4)** — `AskUserQuestion` + "uma pergunta por vez" lowercase substring, `.lint-backup` referenciado, `lint-decisions.md` referenciado, envelope de stack `TS/TSX/JS/JSX`. Backpressure 4/4: typecheck limpo, lint placeholder ok, **669/669 tests verdes** (636 prévios + 33 novos), build placeholder ok. **Note:** install.sh já cobre o layout — `install_category_subdirs "skills"` copia `skills/lint/` para `<target>/skills/lint/`, e `install_cli` aninha `cli/` em `<target>/skills/lint/cli/`, resolvendo `${CLAUDE_PLUGIN_ROOT:-$HOME/.claude}/skills/lint/cli/src/index.ts` byte-a-byte com o preâmbulo do SKILL.md.
- [x] Criar `commands/lint/setup.md` (chama detect-* → AskUserQuestion (estágio, coverage) → install-* em ordem) (why: SPEC §2 `/lint:setup`). Implementado em `commands/lint/setup.md` (87 linhas, abaixo do teto de 100 do SPEC §4 line 296). YAML frontmatter com 4 campos: `name: lint:setup`, `description` começando por "Use when…" (SPEC §4 line 294 — ativação na primeira posição) cobrindo gatilhos verbais ("set up lint", "install oxlint", "configure oxc") e o slash command literal, `allowed-tools: Bash, AskUserQuestion, SlashCommand, Read` (mínimo necessário do orquestrador — `Bash` para o preâmbulo `QUALY_CLI`, `AskUserQuestion` para confirmações 1-por-vez, `SlashCommand` para delegar a `/lint:rollback` / `/lint:status`, `Read` para auditar manifest), `argument-hint: (none)`. Corpo organizado nas 6 seções fixas do SPEC §4 line 295 em ordem: **Visão Geral** (4 etapas: detecção → confirmação → instalação por camadas → verificação; explicita reuso do preâmbulo `QUALY_CLI=` definido em SKILL.md, alinhado a PLAN §Resolução do CLI line 192 "definido uma vez em SKILL.md e reusado"), **Quando usar** (4 cenários: TS/JS sem oxc, greenfield novo, brownfield com ESLint+Prettier/Biome/dprint roteia para `/lint:rollback` ou `lint-migrator` SPEC §7.2, projeto com coverage existente SPEC §7.3), **Quando NÃO usar** (4 cenários de recusa: stack bloqueada exit 2, working tree sujo, repo já configurado pelo qualy via `.lint-manifest.json`, mudança de versões/CI fora de escopo), **Fluxo** (10 passos numerados: `detect-stack` → `git-clean-check` → `detect-existing-linter` → `detect-test-runner` → `detect-stage` → 3 perguntas via `AskUserQuestion` (estágio com 4 opções listando greenfield/brownfield-moderate/legacy, coverage só se runner≠none, plano antes de aplicar) → 6 chamadas `install-*` em ordem determinística com `--strict` (deps → oxlint → hook → husky → coverage → scripts) → pós-condição rodando `status` sem auto-commit), **Mapeamento de exit codes** (cobrindo 0/1/2/3/4/5/70 de `lib/exit-codes.ts` com ações user-friendly), **Trade-offs** (3: determinismo > flex, sem auto-commit SPEC §6 Never line 416, `--strict` em todas as escritas), **Verificação** (smoke + E2E PLAN §Fase 2 + manifest viabilizando uninstall/rollback) e **Referências** cross-ref a SPEC §2/§6/§7.1, PLAN §Fase 2 + §Resolução do CLI, SKILL.md e ADR 0006. **Decisões de design**: (i) preâmbulo `QUALY_CLI=` aparece literal no fluxo (não só em prosa) para facilitar copy-paste pelo modelo, mas a documentação completa fica em SKILL.md (evita drift duplo); (ii) substituição de linter prévio é roteada para `/lint:rollback` ou `lint-migrator` — esse comando NÃO toca arquivos de outros linters (que é responsabilidade do migrator em Fase 3); (iii) ordering `deps → oxlint → hook → husky → coverage → scripts` justificada inline (deps antes de qualquer config, scripts por último porque referenciam paths das deps); (iv) Pergunta 2 condicional a `runner !== "none"` (evita fazer pergunta de coverage em projeto sem test runner); (v) cada install-* recebe `--strict` propagando defesa em profundidade sobre `git-clean-check` (já feito no passo 2). Suite de contrato `cli/tests/unit/command-lint-setup-md.test.ts` (40 tests em 8 describe blocks): **file hygiene (4)** — LF only, single trailing newline, sem BOM, ≤100 linhas; **YAML frontmatter (6)** — começa com `---`, `name: lint:setup` exato, description com prefix `^(Use when|Triggered by)` (anchor SPEC §4 line 294), description menciona `/lint:setup`, `allowed-tools` declarado e contém `Bash` + `AskUserQuestion`; **required sections (7)** — H2 exato para Visão Geral / Quando usar / Quando NÃO usar / Fluxo / Trade-offs / Verificação + assertion de ordem via `indexOf` monotônico; **Resolução do CLI preamble (3)** — regex exato `QUALY_CLI=` literal (PLAN line 196), `node --experimental-strip-types "$QUALY_CLI"` literal (PLAN line 198), referência a SKILL.md como fonte canônica do preâmbulo; **detection step coverage (5)** — `detect-stack`, `git-clean-check`, `detect-existing-linter`, `detect-test-runner`, `detect-stage` mencionados; **install order (7)** — 6 installers individuais + assertion de ordem via `indexOf` monotônico (deps→oxlint→hook→husky→coverage→scripts); **exit code mapping (2)** — `2` para unsupported-stack, `3` + "git stash" para dirty-tree (SPEC §6 Always); **global conventions SPEC §6 (6)** — `AskUserQuestion` + "uma pergunta por vez" lowercase, "não commit" lowercase (SPEC §6 Never line 416), `--strict` mencionado, envelope `TS/TSX/JS/JSX`, três stage names (greenfield/brownfield-moderate/legacy) enumerados, rota `/lint:rollback|lint-migrator` para substituição de linter prévio. **Note**: `install.sh` já cobre o layout — `install_category_subdirs "commands"` copia `commands/lint/setup.md` para `<target>/commands/lint/setup.md` (instalação validada em iteração prévia da Fase 0). Backpressure 4/4: typecheck limpo, lint placeholder, **709/709 tests verdes** (669 prévios + 40 novos), build placeholder.
- [x] Criar `agents/lint-detector.md` (wrapper sobre `detect-*` retornando sumário ≤30 linhas) (why: SPEC §2 + §4 Subagents). Implementado em `agents/lint-detector.md` (86 linhas, ~57% do teto de 150 do SPEC §4 line 296). Frontmatter de subagent Claude Code (campo `tools:`, não `allowed-tools:`): `name: lint-detector`, `description` começando por "Use when…" (SPEC §4 line 294) listando os signals coletados e os callers `/lint:setup` + `/lint:status`, `tools: Bash, Read` (read-only por SPEC §4 line 302 — explicitamente exclui `Write`/`Edit`/`MultiEdit`). Reusa o preâmbulo `QUALY_CLI` definido em `skills/lint/SKILL.md` (PLAN §Resolução do CLI). Corpo organizado nas 6 seções fixas do SPEC §4 line 295: **Visão Geral** (responsabilidade única: detectar; nunca instalar nem migrar — SPEC §4 line 302), **Quando usar** (3 cenários: pré-`/lint:setup|audit|update`, `/lint:status` com detalhes, auditoria de linter externo), **Quando NÃO usar** (4 cenários: stack já bloqueada, tree limpo cacheado, escrita/aplicação, projetos não-TS/JS), **Fluxo** (6 passos: `detect-stack` early-exit em `2` → `git-clean-check` continua mesmo em `3` para coletar todos os sinais → `detect-existing-linter` → `detect-test-runner` → `detect-stage` → sumário fixo) + bloco de **formato fixo do sumário** com 14 linhas estruturadas (`stack`/`git`/`linters`/`formatters`/`runner`/`coverage thresholds`/`stage`/`signals` aninhados/`recommendation`), **Trade-offs** (4: read-only > orquestração total, sumário fixo > log dump, continua após exit `3` para evitar re-detecção, exit code do mais severo `2 > 3 > 5 > 1 > 70 > 0`), **Verificação** (3 smokes contra fixtures `greenfield-ts`/`unsupported-python`/`brownfield-eslint-prettier` + budget ≤30 linhas). **Decisões de design**: (i) usa `tools:` (formato Claude Code subagent) em vez de `allowed-tools:` (formato skill/command) — alinhado a SPEC §3 line 99-103 que enumera `agents/` como diretório separado de `skills/` e `commands/`, e a `install.sh` `install_agent_files` que trata `.md` flat em `agents/`; (ii) early-exit em `detect-stack` mas continua após `git-clean-check` `3` (informação é barata; o pai pode pedir `git stash` em paralelo evitando re-detecção); (iii) exit code do mais severo previne ambiguidade entre stack-bloqueada (abort total) e tree-sujo (recuperável); (iv) sumário em ≤30 linhas via formato fixo de 14 linhas — deixa headroom para edge cases como múltiplos blockers ou linters detectados; (v) cross-refs explícitas a `lint-installer` + `lint-migrator` reforçam a separação de responsabilidades. Contrato lockado em `cli/tests/unit/agent-lint-detector-md.test.ts` (36 testes em 7 describe blocks): **file hygiene (4)** — LF only, single trailing newline, sem BOM, ≤150 linhas; **YAML frontmatter (7)** — começa com `---`, `name: lint-detector` exato, description com prefix `^(Use when|Triggered by)` (anchor SPEC §4 line 294), description menciona `/lint:(setup|status|audit|update)`, `tools:` declarado, `tools` contém `Bash`, `tools` NÃO contém `Write`/`Edit`/`MultiEdit` (read-only assert SPEC §4 line 302); **required sections (7)** — H2 exato para Visão Geral / Quando usar / Quando NÃO usar / Fluxo / Trade-offs / Verificação + assertion de ordem via `indexOf` monotônico; **Resolução do CLI preamble (3)** — regex exato `QUALY_CLI=` literal (PLAN line 196), `node --experimental-strip-types "$QUALY_CLI"` literal (PLAN line 198), cross-ref a SKILL.md; **detector coverage (6)** — 5 detectores individuais + assertion de ordem via `indexOf` monotônico (`detect-stack` → `git-clean-check` → `detect-existing-linter` → `detect-test-runner` → `detect-stage`); **exit code mapping (2)** — `2` + "unsupported|não suportad" para early-exit, `3` + "dirty|sujo" para continuação; **summary contract (8)** — budget literal `≤30`, "read-only" + "não modifica/escreve/instala" lowercase, três stage names enumerados, três runner values enumerados, "sinais brutos|signals" presente (SPEC §6 Always), envelope `TS/TSX/JS/JSX`, delegação explícita a `lint-installer` + `lint-migrator`. **Note**: `install.sh` `install_agent_files` já cobre o layout — copia `agents/lint-detector.md` flat para `<target>/agents/lint-detector.md` (SPEC §3 line 185 + line 99-103). Backpressure 4/4: typecheck limpo, lint placeholder, **745/745 tests verdes** (709 prévios + 36 novos), build placeholder.
- [x] Criar `agents/lint-installer.md` (wrapper sobre `install-*` por camadas com opt-out) (why: SPEC §2 + §4 Subagents). Implementado em `agents/lint-installer.md` (99 linhas, ~66% do teto de 150 do SPEC §4 line 296). Frontmatter de subagent Claude Code (campo `tools:`): `name: lint-installer`, `description` começando por "Use when…" (SPEC §4 line 294) listando as 6 camadas de install (`install-deps`, `install-oxlint`, `install-hook`, `install-husky`, `install-coverage`, `install-scripts`) e os 4 callers (`/lint:setup`, `/lint:update`, `/lint:rules:add`, `/lint:rules:remove`), `tools: Bash, Read`. **Decisão de tools**: SPEC §4 line 302 autoriza Write/Edit para installer/migrator, mas o desenho ADR 0006 (CLI determinístico / harness fino) determina que TODA escrita passa pelo CLI via `Bash` — o subagent só precisa de `Bash` + `Read` (este último para conferir `.lint-manifest.json` na pós-condição). Reuso explícito do preâmbulo `QUALY_CLI` definido em SKILL.md (PLAN §Resolução do CLI). Corpo organizado nas 6 seções fixas do SPEC §4 line 295 em ordem: **Visão Geral** (responsabilidade única: instalar; nunca detectar/migrar/auditar — single responsibility SPEC §4 line 301), **Quando usar** (4 cenários: pós-detecção em `/lint:setup`, mudança de estágio em `/lint:update`, regeração de presets em `/lint:rules:{add,remove}`, install de subset), **Quando NÃO usar** (4 cenários de pré-condição faltando: sem detecção prévia, dirty tree sem `--strict=false` explícito, linter prévio não removido, stack bloqueada), **Fluxo** (parâmetros recebidos: `stage` obrigatório, `runner` obrigatório, `skip[]` opcional, `strict` default true; ordem canônica das 6 camadas com semântica per-layer; pós-condição: `Read` do manifest) + bloco de **formato fixo do sumário** com 11 linhas estruturadas (`stage`/`runner`/`strict`/`layers` aninhados/`manifest entries`/`failed_at`/`recommendation`), **Trade-offs** (5: ordem fixa, opt-out > opt-in, CLI-only writes ADR 0006, abort-na-primeira-falha, `--strict` default true), **Verificação** (3 smokes contra fixtures + budget ≤30 linhas), **Referências**. **Decisões de design**: (i) `tools: Bash, Read` (não Write/Edit) para alinhar com ADR 0006 mesmo com SPEC §4 autorizando — ADR mais estrito vence; (ii) opt-out via `skip[]` (não opt-in via `only[]`) — default inclusivo previne que parent esqueça uma camada; (iii) abort-na-primeira-falha em vez de continuar-acumulando — manifest preserva estado parcial para `/lint:rollback`; (iv) parâmetros `stage`/`runner` declarados como obrigatórios reforça que `lint-detector` é pré-condição; (v) per-layer status enum (installed/skipped/opted-out/failed/created/updated/unchanged/kept/noop/conflicts) reflete 1:1 a action enum de cada `install-*` correspondente do CLI. Contrato lockado em `cli/tests/unit/agent-lint-installer-md.test.ts` (38 testes em 8 describe blocks): **file hygiene (4)** — LF only, single trailing newline, sem BOM, ≤150 linhas; **YAML frontmatter (6)** — começa com `---`, `name: lint-installer` exato, description com prefix `^(Use when|Triggered by)` (anchor SPEC §4 line 294), description menciona `/lint:(setup|update|rules)`, `tools:` declarado, `tools` contém `Bash`; **required sections (7)** — H2 exato para Visão Geral / Quando usar / Quando NÃO usar / Fluxo / Trade-offs / Verificação + assertion de ordem via `indexOf` monotônico; **Resolução do CLI preamble (3)** — regex exato `QUALY_CLI=` literal (PLAN line 196), `node --experimental-strip-types "$QUALY_CLI"` literal (PLAN line 198), cross-ref a SKILL.md; **install-* layer coverage (7)** — 6 camadas individuais + assertion de ordem via `indexOf` monotônico (deps → oxlint → hook → husky → coverage → scripts); **opt-out per layer (3)** — substring `opt-out|skip` lowercase, semântica `skip` cabeada a uma das 6 camadas, default = todas as camadas; **exit handling (2)** — `--strict` mencionado, abort-on-failure semântica; **summary contract (6)** — budget literal `≤30`, três stage names enumerados (greenfield/brownfield-moderate/legacy), três runner values (vitest/jest/none), `.lint-manifest.json` referenciado, delegação explícita a `lint-detector` + `lint-migrator`, ADR 0006 / CLI mencionado para writes. **Note**: `install.sh` `install_agent_files` já cobre o layout — copia `agents/lint-installer.md` flat para `<target>/agents/lint-installer.md`. Backpressure 4/4: typecheck limpo, lint placeholder, **783/783 tests verdes** (745 prévios + 38 novos), build placeholder.
- [x] Escrever teste e2e em `cli/tests/e2e/setup-greenfield.test.ts` rodando `setup` no fixture e validando todos os artefatos do SPEC §7.1 (why: PLAN §Fase 2 verificação). Implementado em `cli/tests/e2e/setup-greenfield.test.ts` (2 `it`s em 1 `describe`). O teste principal espelha o orquestrador `commands/lint/setup.md` chamando cada `install-*` na ordem canônica (deps → oxlint → hook → husky → coverage → scripts) contra um clone real da fixture (cp + `git init` + commit determinístico via `materializeFixture`). **Tratamento do `install-deps`**: o subprocess do package manager é stubado via `runFn` injetada (retorna `{ok:true,exitCode:0}` sem spawnar) — alternativa "deixar `npm install` rodar de verdade" foi rejeitada porque depende de rede e cache de registry imprevisíveis em CI; o stub valida o caminho instalável (`action:"installed"`, todos os `DEFAULT_DEPS` em `installed[]`, `recorded === 4`) sem mexer em `node_modules/`. Os outros 5 layers rodam contra FS real. **Asserções dos artefatos SPEC §7.1**: (i) `oxlint.fast.json` e `oxlint.deep.json` byte-igual aos presets bundled em `cli/src/presets/oxlint/greenfield.{fast,deep}.json` (lock contra drift de preset); (ii) `.claude/hooks/post-edit.sh` contém shebang bash + `set -euo pipefail` + referência a `oxlint.fast.json`, e `statSync(...).mode & 0o100 !== 0` confirma o exec bit do owner; (iii) `.claude/settings.json` parseia como JSON, `hooks.PostToolUse[]` contém entry com `matcher === "Write|Edit|MultiEdit"` cujo `hooks[0].command` referencia o script (`action: "created"` na 1ª run, `"unchanged"` na re-run); (iv) `.husky/pre-commit` contém substring `lint-staged`, e (v) `.lintstagedrc.js` (não `.mjs` — `package.json#type === "module"` no fixture) é byte-igual a `cli/src/templates/lintstagedrc.example.js`; (vi) `vitest.config.ts` criado do skeleton tem `lines:90 / functions:90 / branches:80 / statements:90` (greenfield SPEC §3) inline + `provider: "v8"`, `action === "created"`, `warnOnly:false`; (vii) `package.json#scripts` recebe `lint` / `lint:deep` / `format` / `coverage` exatos do template + `coverage_by_runner.vitest`, **preservando o `start` script pré-existente do fixture** (sanity check de merge não-destrutivo) e o `type: "module"` top-level. **Manifest sanity**: indexa todos os 9 artefatos (incluindo 4 entries virtuais `package.json#devDependencies/<dep>` kind=`dep` merged=true para `oxlint`/`oxfmt`/`quality-metrics`/`ts-morph`) com `kind` correto: `preset` para os 2 oxlint, `hook` para `post-edit.sh`, `husky`/`lintstaged` para os 2 husky artifacts, `coverage` (merged=undefined porque qualy criou o arquivo do skeleton — `loadManifest` só inclui `merged` quando true; checked via `?? false`), `settings`/`scripts` merged=true para os arquivos do usuário; o próprio `.lint-manifest.json` NÃO está indexado (guard contra recursão de auto-record). **Idempotência** (4 sub-checks pós-1ª run): re-rodar `installOxlint` mantém `oxlint.fast.json` byte-idêntico; `installHook` retorna `settings.action === "unchanged"`; `installHusky` retorna `husky.action === "unchanged"` + `lintstaged.action === "kept"`; `installScripts` retorna `action === "noop"` + `added: []`. **2º teste — strict gate**: dirties o tree (`writeFileSync` de um `untracked-file.txt`) e chama `installOxlint({strict:true})`; assere `error === "write_failed"` + `reason.contains("working tree is dirty")`, e que NEM `oxlint.fast.json` NEM `.lint-manifest.json` chegaram ao disco — defesa em profundidade SPEC §6 contra escritas em working tree sujo. **Wiring no vitest config**: `vitest.config.ts` agora inclui `cli/tests/e2e/**/*.test.ts` em adição ao `cli/tests/unit/**` (atualizada doc inline do header explicando o tradeoff: e2e suites pagam round-trip de `cp + git init + commit` por `it`, mas o set atual ainda fecha em ~14s combinado, e a backpressure suite agora exercita os artefatos automaticamente — wiring de `pnpm test:e2e` separado fica para a tarefa de Fase 7 line 137). **Decisões de design**: (i) chamar funções `install-*` puras em vez de spawnar `cli/src/index.ts` via `execFileSync` — evita acoplar o teste ao parser argv e ao formato exato do JSON em stdout, e mantém o teste rápido; (ii) `--strict` deliberadamente NÃO usado no flow principal (cada layer suja o tree para o próximo; em produção o harness rodaria `--strict` apenas na 1ª camada ou commitaria entre layers — o 2º teste cobre o caminho strict separadamente); (iii) `__dirname` substituído por `dirname(fileURLToPath(import.meta.url))` — projeto é ESM puro (PLAN §ADR 0007). Backpressure 4/4: typecheck limpo, lint placeholder, **785/785 tests verdes** (783 prévios + 2 novos), build placeholder. Total da suite ~14s (e2e adiciona ~6s de I/O git + FS).

## Priority 4: Fase 3 — Migration / backup

- [x] Implementar `cli/src/commands/backup/create.ts` (cria `.lint-backup/<ISO-timestamp>/` com cópias de arquivos passados via `--files`) (why: SPEC §6 Always – backup nomeado). Implementado como `backupCreate(opts, deps?)` puro + handler `runBackupCreate(argv)` + helper exposto `toSafeTimestamp(date)`. **Algoritmo**: (i) valida `files` não-vazio (`USAGE_ERROR files_empty`); (ii) deriva timestamp filesystem-safe via `toSafeTimestamp(now)` — `Date#toISOString()` com `:` e `.` substituídos por `-` (ex: `2026-05-03T12-30-45-123Z`) — `--ts <override>` permite e2e/idempotência determinística; (iii) **valida todos os paths up-front** antes de qualquer write (atômico em invalid-input): `resolveSafePath` rejeita absolute paths e `..` escape (path_invalid), `existsFn` confirma source presente (file_not_found); (iv) por arquivo, lê bytes via `readFileFn` (read_failed em throw) e escreve via `safeWriteFile(cwd, ".lint-backup/<ts>/<src-rel>", content, {kind:"backup", strict})` — preserva árvore de diretórios sob `<src-rel>` para que `backup-restore` possa repor cada arquivo na localização original byte-a-byte (SPEC §7.2). **Manifest**: cada arquivo gera entry com `kind: "backup"` (novo, adicionado a `ManifestEntryKind` em `fs-safe.ts` — owned-by-qualy, sem `merged:true`; uninstall deleta por default ou preserva sob `--keep-backup` na task seguinte). **Idempotência**: default = timestamp único por chamada (backups acumulam — uma snapshot por tentativa de migração); `--ts` explícito reescreve byte-a-byte e mantém uma única manifest entry por path (recordEntry deduplica). **Args**: `--files <json>` (array de strings, mesmo padrão de `install-deps --deps`), `--cwd`, `--ts <override>`, `--strict`, `--help/-h`. **Exit codes**: `OK` sucesso; `USAGE_ERROR` (4) flag inválida / `files_empty` / `files_invalid` / `path_invalid`; `RECOVERABLE_ERROR` (1) `file_not_found` / `read_failed` / generic `write_failed`; `DIRTY_TREE` (3) `--strict` + tree sujo (write_failed com reason "working tree is dirty"). **Output**: `{ ok, cwd, timestamp, dir, backed_up: [{src,dest,bytes}] }`. **Wired no dispatcher** via `HANDLER_OVERRIDES` Map. **Suite `cli/tests/unit/backup-create.test.ts` (27 testes em 6 describe blocks)**: `toSafeTimestamp` (3 — replaces colons+dots, sortable lexically, sem chars inválidos no Windows); happy path (5 — single file, nested dirs preservados, manifest entries kind=backup, --ts override, default ts derivado de now); error paths (7 — files_empty, files_invalid em string vazia, path_invalid em ../escape, path_invalid em absolute path, file_not_found atômico (nem o presente é escrito), read_failed em readFileFn throw, write_failed/DIRTY_TREE em strict+dirty); idempotência (2 — same --ts dedup manifest + sobrescreve bytes, default ts diferente entre calls acumula); parseBackupCreateArgs (10 — requires --files, parses --files JSON, parses --cwd/--ts/--strict em qualquer ordem, malformed JSON, non-array, non-string element, empty string element, missing values for --cwd/--files/--ts, unknown flag, --help/-h). Backpressure 4/4: typecheck limpo, lint placeholder, **812/812 tests verdes** (785 prévios + 27 novos), build placeholder.
- [x] Implementar `cli/src/commands/backup/list.ts` (lista timestamps + arquivos) (why: PLAN §Contratos CLI). Implementado como `backupList(opts, deps?)` puro + handler `runBackupList(argv)`. **Source of truth**: `.lint-manifest.json` via `loadManifest()` — entries com `kind === "backup"` agrupadas por timestamp extraído do path `.lint-backup/<ts>/<src>`. Decisão: NÃO walk de FS sob `.lint-backup/` — manifest é o que `uninstall`/`backup-restore` honra, então listar o que o manifest sabe casa exatamente com o que `/lint:rollback` pode acionar. **`present: bool`** por arquivo: stata o destino do backup via `existsFn` (default `existsSync`) — robusto a deleção manual sob `.lint-backup/` permitindo o harness avisar antes de roteamento para um backup quebrado. **Sort**: backups descending lexicalmente por timestamp (mais recente primeiro — `toSafeTimestamp` gera strings sortable, então `/lint:rollback` defaulta a `backups[0]`); arquivos dentro de cada backup ascending por `src` para output determinístico. **Splitter defensivo** `splitBackupPath()`: rejeita paths que não começam com `.lint-backup/`, sem `/` após `<ts>`, ou com slash terminal — entries `kind:"backup"` com paths bogus (forward-compat ou corrupção) são silenciosamente droppadas em vez de derrubar o comando. Manifest ausente / malformed → `{ok:true, backups:[]}` (read-only nunca falha em ausência). Output `{ok, cwd, backups: [{timestamp, dir, files: [{src, dest, present}]}]}`. Exit codes: `OK` em sucesso, `USAGE_ERROR` em flag inválida. Args minimal: `--cwd`, `--help/-h`. **Wired no dispatcher** via `HANDLER_OVERRIDES` Map. **Suite `cli/tests/unit/backup-list.test.ts` (15 testes em 5 describe blocks)**: empty/absent manifest (3 — sem manifest → empty, manifest sem backup entries → empty, manifest malformed → empty); happy path (5 — single backup grouping, nested src preservation, sort descending across 3 timestamps fora de ordem, files-within-backup sort por src, ignora non-backup entries `preset`/`scripts`); FS presence (1 — present:false após deleção manual de um arquivo do backup); defensive splitter (1 — 4 paths bogus em uma única call: sem prefix, só prefix, só dir após `<ts>`, sem slash após `<ts>` — todos droppados); parseBackupListArgs (5 — defaults, --cwd, unknown flag, --cwd sem valor, --help/-h help sentinel). Backpressure 4/4: typecheck limpo, lint placeholder, **827/827 tests verdes** (812 prévios + 15 novos), build placeholder.
- [x] Implementar `cli/src/commands/backup/restore.ts` (restore byte-a-byte do timestamp escolhido) (why: SPEC §7.2 acceptance – restaura byte-a-byte). Implementado como `backupRestore(opts, deps?)` puro + handler `runBackupRestore(argv)`. **Source of truth**: `.lint-manifest.json` (entries com `kind:"backup"` cujo path começa com `.lint-backup/<ts>/`) — espelha o desenho de `backup-list`: o que o manifest sabe é exatamente o que restore pode reverter (nunca walk de FS sob `.lint-backup/`). **Algoritmo**: (i) valida `--ts` não-vazio (`USAGE_ERROR timestamp_empty`); (ii) carrega manifest e filtra entries `kind:"backup"` cujo `splitBackupPath().timestamp === ts`; zero matches → `timestamp_not_found` (RECOVERABLE_ERROR — manifest ausente, ts inexistente ou apenas decoy entries); (iii) se `--files <json>` provido restringe ao subset (subset com path ausente → `subset_not_in_backup` USAGE_ERROR); empty array trata como "restore all" alinhando com semântica do default; (iv) **valida todos os backup files existem on-disk up-front** (atomic-ish, mesmo padrão de `backup-create`) — um faltando → `backup_file_missing`, NADA é escrito; (v) por entry, lê bytes de `.lint-backup/<ts>/<src>` e escreve em `<src>` via `safeWriteFile({skipManifest:true, strict})`. **`skipManifest`** é nova flag em `SafeWriteOptions` (extensão de `lib/fs-safe.ts`): destinos do restore são arquivos do USUÁRIO (não qualy-owned), então o manifest não pode reivindicar ownership deles; entries `kind:"backup"` permanecem intocadas para auditoria/listagem e o user file recém-restaurado não vira "qualy artifact". `--strict` propaga para `safeWriteFile` (DIRTY_TREE protege contra clobbar trabalho do usuário não-commitado). **Ordering**: default sort por `src` ASC para output determinístico; `--files` preserva ordem do request (caller controla). **Output**: `{ok, cwd, timestamp, dir, restored: [{src, from, bytes}]}`. **Exit codes**: `OK` sucesso; `USAGE_ERROR` (4) timestamp_empty / files_invalid / path_invalid / subset_not_in_backup / unknown flag; `RECOVERABLE_ERROR` (1) timestamp_not_found / backup_file_missing / read_failed / generic write_failed; `DIRTY_TREE` (3) `--strict` + tree sujo. **Wired no dispatcher** via `HANDLER_OVERRIDES`. **Suite `cli/tests/unit/backup-restore.test.ts` (26 testes em 4 describe blocks)**: happy path (6 — single file byte-exact, nested dirs preservados, manifest NÃO ganha entries para destinos restaurados (kinds permanecem só `backup`), pick correto entre múltiplos timestamps, ignora `kind!=backup` e timestamps diferentes, ordering determinístico por src); subset `--files` (4 — restaura apenas requested + outros intocados, preserva ordem do request, subset_not_in_backup atomic-ish, empty array vira default); error paths (6 — timestamp_empty, manifest absent → timestamp_not_found, manifest sem matching entries → timestamp_not_found, backup_file_missing atomic-ish (NADA restaurado mesmo o sibling presente), read_failed via readFileFn throw, write_failed/DIRTY_TREE em strict+dirty); parser (10 — requires --ts, --ts isolado, todos os flags em qualquer ordem, malformed JSON, non-array, non-string element, empty string, missing values for --cwd/--ts/--files, unknown flag, --help/-h help sentinel). **Smoke test e2e contra tmp dir real**: 1) `git init` + commit + escrita de `.eslintrc.yml`; 2) `backup-create --ts ts1 --files '[".eslintrc.yml"]'` → manifest + cópia em `.lint-backup/ts1/`; 3) muta `.eslintrc.yml` no original; 4) `backup-restore --ts ts1` → 32 bytes idênticos antes/depois (verificado via `cat`); 5) `backup-restore --ts NOPE` → `{"error":"timestamp_not_found"}` exit 1. Backpressure 4/4: typecheck limpo, lint placeholder, **853/853 tests verdes** (827 prévios + 26 novos), build placeholder.
- [x] Implementar `cli/src/commands/uninstall.ts` (lê `.lint-manifest.json` e remove tudo; flag `--keep-backup`) (why: SPEC §2 `/lint:uninstall`). Implementado como `uninstall(opts, deps?)` puro + handler `runUninstall(argv)`. **Source of truth**: `.lint-manifest.json`. Particiona entradas em três classes: (i) **owned files** (kind ∈ {preset, hook, husky, lintstaged, decisions, template, other} e `merged !== true`) → deletadas via `safeIO.removeFn`; (ii) **backup snapshots** (kind=backup) → deletadas por padrão; com `--keep-backup` preservadas (arquivos + entries do manifest); (iii) **merged/virtual** (`merged === true` → settings/scripts/coverage; ou `kind === "dep"` → paths virtuais `package.json#devDependencies/<name>`) → não deletadas, surfaced em `merged_kept` para a harness limpar via pkg-manager remove + edits cirúrgicos. Manifest deletado quando `remaining.length === 0`; reescrito caso contrário (mantém entries de backup `--keep-backup` + merged + dep). Saída: `{ ok, cwd, removed: string[], kept_backup: bool, merged_kept: [{path, kind}] }`. Exit codes: OK / RECOVERABLE_ERROR (manifest_missing, remove_failed) / USAGE_ERROR (path_invalid, unknown flag). Wired em `cli/src/index.ts`. 20 testes unit em `cli/tests/unit/uninstall.test.ts`. Follow-up identificado: surgical key-removal em settings/scripts/coverage e `pkg-manager remove` para entries `kind:dep` ainda não implementados — ficam para um wrapper na harness `commands/lint/uninstall.md` ou tarefas futuras.
- [x] Criar `commands/lint/uninstall.md` orquestrando uninstall + oferta de restore (why: SPEC §2). Implementado em `commands/lint/uninstall.md` (81 linhas, 81% do teto de 100 do SPEC §4 line 296). Frontmatter de slash command com 4 campos: `name: lint:uninstall`, `description` começando por "Use when…" (SPEC §4 line 294) cobrindo gatilhos verbais ("uninstall lint", "remove oxlint", "tear down qualy") + slash command literal + menção explícita a `--keep-backup` e `/lint:rollback`, `allowed-tools: Bash, AskUserQuestion, SlashCommand, Read` (mínimo do orquestrador), `argument-hint: [--keep-backup]` (usuário pode pular a Pergunta 1 passando o flag direto). Reusa preâmbulo `QUALY_CLI` definido em SKILL.md (PLAN §Resolução do CLI). Corpo nas 6 seções fixas do SPEC §4 line 295: **Visão Geral** (4 etapas: inventário read-only → confirmação → remoção → follow-up), **Quando usar** (4 cenários: uninstall total, brownfield com restore via /lint:rollback, re-setup com mudança de estágio, idempotência após falha parcial), **Quando NÃO usar** (4 cenários: sem manifest, desabilitar temporariamente via stash, remoção de rule/threshold isolada via /lint:rules:remove ou /lint:update, restore-only via /lint:rollback), **Fluxo** (10 passos: Read manifest → backup-list → git-clean-check → imprimir plano com 3 buckets do manifest particionados por kind (owned/backup/merged-virtual) → Pergunta 1 preservar snapshots default Recommended → Pergunta 2 aplicar plano → uninstall [--keep-backup] single-call → follow-up por merged_kept (kind:dep → pkg-manager remove; merged:true → edição cirúrgica) → oferta de /lint:rollback se backups remanescentes → status pós-condição sem auto-commit), **Mapeamento de exit codes** (0 ok / 1 manifest_missing|remove_failed / 3 dirty-tree reservado para evolução / 4 path_invalid|usage), **Trade-offs** (4: merged_kept não auto-deletado segurança>completude, default=preservar reversibilidade>limpeza, sem auto-commit SPEC §6 Never line 416, single-call ao CLI atomicidade do manifest), **Verificação** (smoke --help + E2E SPEC §7.2 brownfield setup→uninstall→rollback git diff vazio + manifest ausente após clean uninstall), **Referências** com cross-refs a SPEC §2/§6/§7.2, PLAN §Fase 3 + §Resolução do CLI, SKILL.md, commands/lint/rollback.md (próxima task) e ADR 0006. **Decisões de design**: (i) preâmbulo `QUALY_CLI=` aparece literal no Fluxo (copy-paste-friendly) mas a documentação canônica fica em SKILL.md (sem drift duplo); (ii) Pergunta 1 default = preservar `.lint-backup/` (Recommended) — reversibilidade > limpeza; usuário pode rodar `uninstall` de novo sem `--keep-backup` quando confiante; (iii) `merged_kept` (settings/scripts/coverage merged=true + kind:dep) NÃO é apagado automaticamente — segurança > completude (settings.json compartilha arquivo com edições do usuário); o follow-up manual é instruído per-entry; (iv) oferta de `/lint:rollback` via `SlashCommand` na etapa 9 quando `backup-list` retornou snapshots — desinstalar oxc raramente é o objetivo final em brownfield; (v) `--keep-backup` exposto também como `argument-hint` para usuários que querem skip da Pergunta 1; (vi) exit `3` documentado mas reservado para evolução futura — `uninstall` v1 não tem `--strict`, mas a defesa em profundidade via `git-clean-check` na etapa 3 ainda protege trabalho não-commitado adjacente. Contrato lockado em `cli/tests/unit/command-lint-uninstall-md.test.ts` (37 testes em 8 describe blocks): **file hygiene (4)** — LF only, single trailing newline, sem BOM, ≤100 linhas; **YAML frontmatter (7)** — começa com `---`, `name: lint:uninstall` exato, description com prefix `^(Use when|Triggered by)` (anchor SPEC §4 line 294), description menciona `/lint:uninstall`, `allowed-tools` declarado e contém `Bash` + `AskUserQuestion`, `argument-hint: [--keep-backup]` exato; **required sections (7)** — H2 exato para Visão Geral / Quando usar / Quando NÃO usar / Fluxo / Trade-offs / Verificação + assertion de ordem via `indexOf` monotônico; **Resolução do CLI preamble (3)** — regex exato `QUALY_CLI=` literal (PLAN line 196), `node --experimental-strip-types "$QUALY_CLI"` literal (PLAN line 198), cross-ref a SKILL.md; **CLI subcommand coverage (6)** — `backup-list`, `uninstall`, `git-clean-check` mencionados + `.lint-manifest.json` referenciado + `.lint-backup` referenciado + `/lint:rollback` como follow-up de restore; **manifest partitioning (3)** — `--keep-backup` mencionado, `merged_kept` referenciado para entries surfacing, follow-up para `kind:dep` via `(npm|pnpm|yarn|bun) remove`; **exit code mapping (3)** — `0` sucesso, `1` + "manifest_missing" lowercase, `3` + "git stash" lowercase (SPEC §6 Always); **global conventions SPEC §6 (4)** — `AskUserQuestion` + "uma pergunta por vez" lowercase, "não commit" lowercase (SPEC §6 Never line 416), envelope `TS/TSX/JS/JSX`, "plano antes de aplicar" semântica (SPEC §6 Always). Backpressure 4/4: typecheck limpo, lint placeholder, **910/910 tests verdes**, build placeholder.
- [ ] Criar `commands/lint/rollback.md` orquestrando `backup-restore` mais recente (why: SPEC §2 `/lint:rollback`)
- [ ] Criar `agents/lint-migrator.md` (wrapper backup + uninstall) (why: SPEC §2 + §4 Subagents)
- [ ] Escrever teste e2e em `cli/tests/e2e/setup-rollback-brownfield.test.ts` validando SPEC §7.2 (why: PLAN §Fase 3 verificação)

## Priority 5: Fase 4 — Audit + recommendations

- [ ] Criar `cli/src/lib/audit-schema.ts` (zod) refletindo o contrato JSON do SPEC §3 (why: PLAN §Verification – schema validação)
- [ ] Implementar `cli/src/commands/audit.ts` (executa oxlint+oxfmt+quality-metrics como subprocessos, agrega em `.lint-audit/<ts>.json` conforme schema) (why: SPEC §2 `/lint:audit` + §3 contrato)
- [ ] Implementar `cli/src/commands/audit-latest.ts` (lê o `.lint-audit/*.json` mais recente) (why: PLAN §Contratos CLI)
- [ ] Criar `docs/recs-heuristics.md` documentando regras determinísticas de geração de recomendação (why: PLAN §Fase 4 + ADR 0008)
- [ ] Implementar `cli/src/commands/recs/generate.ts` (heurísticas de `docs/recs-heuristics.md` → `candidates[]` com `id` estável + `rationale_stub`) (why: PLAN §Contratos CLI – determinismo)
- [ ] Implementar `cli/src/commands/recs/blast-radius.ts` (oxlint dry-run com config proposta vs atual; conta arquivos newly/no-longer violating) (why: SPEC §6 Always – mostrar blast_radius)
- [ ] Implementar `cli/src/commands/recs/apply.ts` (aplica patch de uma rec; faz append em `docs/lint-decisions.md`) (why: SPEC §7.6 acceptance + §6 Always)
- [ ] Criar `commands/lint/audit.md` orquestrando audit (why: SPEC §2)
- [ ] Criar `commands/lint/update.md` orquestrando iteração `recommendations[]` com AskUserQuestion (why: SPEC §2 + acoplamento audit↔update)
- [ ] Criar `agents/lint-auditor.md` (executa `recs-generate`, enriquece `rationale` com contexto do código, persiste `recommendations[]` no `.lint-audit/<ts>.json`; ≤30 linhas no sumário) (why: PLAN §Fase 4 + ADR 0008 – exceção autorizada)
- [ ] Criar `docs/adrs/0008-hybrid-recs-rationale.md` (why: PLAN §Fase 4 lista ADR obrigatório)
- [ ] Escrever teste e2e validando SPEC §7.5 (audit produz JSON válido, exit ≠0 em error-level) e §7.6 (rationale ≠ rationale_stub) (why: PLAN §Fase 4 verificação)

## Priority 6: Fase 5 — Rules management

- [ ] Implementar `cli/src/commands/rules/list.ts` (active, available, disabled com origem e severidade) (why: SPEC §2 `/lint:rules:list`)
- [ ] Implementar `cli/src/commands/rules/explain.ts` (descrição, racional, threshold, links) (why: SPEC §2 `/lint:rules:explain`)
- [ ] Implementar `cli/src/commands/rules/add.ts` (edita preset, append em `docs/lint-decisions.md`; suporta dry-run para blast radius) (why: SPEC §7.9 acceptance)
- [ ] Implementar `cli/src/commands/rules/remove.ts` (edita preset, exige `--reason`, append em `docs/lint-decisions.md`) (why: SPEC §6 Always – registrar motivo)
- [ ] Criar `commands/lint/rules/{list,add,remove,explain}.md` (why: SPEC §2)
- [ ] Escrever testes unit cobrindo idempotência de add/remove e formato de `lint-decisions.md` (why: PLAN §Fase 5 verificação)

## Priority 7: Fase 6 — Report visual

- [ ] Criar `cli/src/report/themes/linear-design-md/{light.css,dark.css,tokens.json}` (why: SPEC §4 default theme)
- [ ] Criar `cli/src/report/index.html` shell (why: SPEC §3 layout report)
- [ ] Criar `cli/src/report/data-loader.ts` (lê `.lint-audit/<latest>.json` + git stats + coverage do runner) (why: SPEC §4 estado fonte)
- [ ] Criar `cli/src/report/components/MetricCard.ts` (vanilla DOM ou Web Component) (why: SPEC §4 vanilla DOM)
- [ ] Criar `cli/src/report/components/ChartLine.ts` (chart.js, tendência por timestamp) (why: SPEC §7.7 acceptance – line de tendência)
- [ ] Criar `cli/src/report/components/ChartTreemap.ts` (chartjs-plugin-treemap por arquivo) (why: SPEC §7.7 acceptance – treemap)
- [ ] Criar `cli/src/report/components/ViolationsTable.ts` (top-N violações com sort) (why: SPEC §7.7 acceptance)
- [ ] Criar `cli/src/report/app.ts` (bootstrap, theme switcher persistente em localStorage, `prefers-reduced-motion`) (why: SPEC §4 a11y + tema)
- [ ] Criar `cli/src/report/server.ts` (`node:http` em `127.0.0.1` em porta livre; serve via esbuild on-the-fly) (why: SPEC §6 Never – não expor além de localhost)
- [ ] Criar `cli/src/report/export.ts` (HTML self-contained; CSS+JS+JSON inline; filtra `process.env`, paths absolutos do autor) (why: SPEC §6 Never – não embutir dados sensíveis)
- [ ] Implementar `cli/src/commands/report/serve.ts` (wraps `report/server.ts`; printa URL + porta) (why: PLAN §Contratos CLI)
- [ ] Implementar `cli/src/commands/report/export.ts` (wraps `report/export.ts` para `quality-report/<ts>.html`) (why: SPEC §7.7 acceptance)
- [ ] Criar `commands/lint/report.md` orquestrando serve → AskUserQuestion (export?) → export opcional (why: SPEC §2 + §6 Ask first)
- [ ] Escrever teste e2e validando export self-contained renderiza idêntico offline (why: PLAN §Fase 6 verificação)

## Priority 8: Fase 7 — Hardening + docs + ADRs restantes

- [ ] Criar `docs/stages.md` (why: SPEC §3 referência da heurística)
- [ ] Criar `docs/thresholds.md` (tabela completa por estágio + métrica) (why: SPEC §3)
- [ ] Criar `docs/coverage.md` (why: SPEC §3 estratégia)
- [ ] Criar `docs/audit-format.md` (contrato JSON) (why: SPEC §3 + §6 Always)
- [ ] Criar `docs/report-design.md` (princípios e como adicionar tema) (why: SPEC §3)
- [ ] Criar `docs/compatibility.md` (matriz de stacks) (why: SPEC §3 + §1 stacks suportadas)
- [ ] Criar `docs/adrs/0001-oxc-only-v1.md` (why: SPEC §3 ADR)
- [ ] Criar `docs/adrs/0002-named-backup-rollback.md` (why: SPEC §3 ADR)
- [ ] Criar `docs/adrs/0003-stage-detection-heuristic.md` (why: SPEC §3 ADR + justificativa thresholds)
- [ ] Criar `docs/adrs/0004-audit-update-coupling.md` (why: SPEC §3 ADR)
- [ ] Criar `docs/adrs/0005-report-ephemeral-server-with-export.md` (why: SPEC §3 ADR)
- [ ] Criar `README.md` raiz (instalação via `./install.sh`, pré-req Node ≥ 22.6, lista de comandos) (why: PLAN §Fase 7)
- [ ] Criar `CHANGELOG.md` inicial (why: SPEC §3 estrutura)
- [ ] Criar roteiros `tests/scenarios/setup-greenfield.md`, `setup-brownfield.md`, `audit-update.md`, `report-export.md`, `unsupported-stack.md` (why: SPEC §5 Tier T2)
- [ ] Adicionar script `pnpm test:e2e` no `cli/package.json` que executa cada fixture e compara contra `EXPECTED.json` versionado (why: PLAN §Verification)
- [ ] Validação final manual contra repo TS real fora dos fixtures (why: PLAN §Fase 7 verificação)

## Blocked

_(none)_
