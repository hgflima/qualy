{
  "name": "ignore-brownfield-fixture",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Synthetic brownfield TypeScript project — oxlint preset already has a user-authored ignorePattern outside qualy markers, exercising the import-on-first-mutation flow (lint-ignore SPEC §10 #6).",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts"
  },
  "engines": {
    "node": ">=22.6.0"
  },
  "devDependencies": {
    "oxlint": "^1.62.0",
    "typescript": "^5.5.4"
  }
}
