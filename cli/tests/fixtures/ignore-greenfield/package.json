{
  "name": "ignore-greenfield-fixture",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Synthetic greenfield TypeScript project with oxlint presets installed and no ignore manifest yet — used as a fixture for qualy ignore-* commands (lint-ignore SPEC §10 #1, #9).",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts"
  },
  "engines": {
    "node": ">=22.6.0"
  },
  "devDependencies": {
    "oxlint": "^1.62.0",
    "typescript": "^5.5.4"
  }
}
