{
  "name": "jest-with-coverage-fixture",
  "private": true,
  "version": "0.1.0",
  "description": "Synthetic TypeScript project with Jest configured and an existing coverage threshold (lines: 60) — fixture for SPEC §7.3 acceptance: detect-test-runner must surface the existing threshold so /lint:setup preserves the user's choice.",
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "engines": {
    "node": ">=22.6.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
