{
  "name": "@qualy/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic CLI for the qualy/lint skill (oxlint + oxfmt + quality-metrics). Runs directly via Node 22.6+ --experimental-strip-types; no build step.",
  "engines": {
    "node": ">=22.6.0"
  },
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"[qualy/cli] test: not yet wired (vitest config + tests added in Fase 1)\" && exit 0",
    "test:e2e": "echo \"[qualy/cli] test:e2e: not yet wired (added in Fase 7)\" && exit 0"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "chartjs-chart-treemap": "^3.1.0",
    "esbuild": "^0.24.0",
    "ts-morph": "^24.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
